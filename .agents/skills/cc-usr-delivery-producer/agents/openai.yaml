interface:
  display_name: "用户满意度交付生产"
  short_description: "分析 Claude 轨迹与产物，逐轮评分并写入 SQLite"
  default_prompt: "使用 $cc-usr-delivery-producer 分析指定批次已完成的 Claude Code 运行，提取会话 ID，逐轮评分并生成证据描述后入库。"

policy:
  allow_implicit_invocation: true
