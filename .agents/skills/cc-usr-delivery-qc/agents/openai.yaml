interface:
  display_name: "用户满意度交付质检"
  short_description: "机械检查字段、枚举、轮次、快照、重复记录及同会话环境一致性"
  default_prompt: "使用 $cc-usr-delivery-qc 对指定 SQLite 批次执行交付结构质检。"

policy:
  allow_implicit_invocation: true
