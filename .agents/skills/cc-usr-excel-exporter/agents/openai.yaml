interface:
  display_name: "用户满意度 Excel 导出"
  short_description: "导出质检通过的 28 列工作簿和对应原始轨迹"
  default_prompt: "使用 $cc-usr-excel-exporter 将指定批次中交付质检通过的记录导出为全新的 28 列 Excel，并把对应原始轨迹复制到批次根目录。"

policy:
  allow_implicit_invocation: true
