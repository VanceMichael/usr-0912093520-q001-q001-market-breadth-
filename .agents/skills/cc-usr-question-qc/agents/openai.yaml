interface:
  display_name: "用户满意度出题质检"
  short_description: "检查首轮 0-1 意图、难度、多样性、仓库快照与可复现性"
  default_prompt: "使用 $cc-usr-question-qc 质检指定 SQLite 批次，确认全部首轮题都是真正的 0-1 代码生成任务，并输出可核验的问题清单。"

policy:
  allow_implicit_invocation: true
