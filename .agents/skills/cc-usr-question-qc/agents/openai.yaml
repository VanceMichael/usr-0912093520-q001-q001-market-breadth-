interface:
  display_name: "用户满意度出题质检"
  short_description: "检查批次内及跨批次题目是否重复或套用同一模板"
  default_prompt: "使用 $cc-usr-question-qc 检查指定 SQLite 批次与完整题库是否存在重复题或模板化改写，并输出可核验的相似度证据。"

policy:
  allow_implicit_invocation: true
