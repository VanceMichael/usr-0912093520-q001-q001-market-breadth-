# 使用说明

日常只需要按下面五步操作。题目、质检状态、运行登记和 Excel 字段都保存在根目录 `production.sqlite3`。

## 1. 填写一次连接配置

打开根目录 `.env`，填写中转站提供的 URL、模型名和 Key：

```dotenv
CC_SWITCH_BASE_URL=https://你的中转站地址
CC_SWITCH_MODEL=你的模型名
CC_SWITCH_API_KEY=你的Key
```

不需要再打开 CC Switch 客户端。URL 必须是中转站提供的 Claude/Anthropic 兼容地址。

每次启动题目时，程序都会重新读取这三项，并通过环境变量交给 Claude Code。它们不会作为 Claude 命令行参数，不会写入 SQLite、题目目录或启动记录。不要把 `.env` 上传或提交到仓库。

## 2. 创建一批题目

你不需要自己建批次目录。直接在本项目对话里说：

```text
使用 $cc-usr-question-author 创建批次。
批次名：0911
题目数量：10
出题要求：这里写业务方向、技术栈、题型比例、仓库来源等要求。
严格遵守 项目规范.md。创建完成后运行出题机械质检，不要启动目标模型。
```

系统会创建：

```text
0911/
  q001/                 第 1 题的实际工作目录
  q002/                 第 2 题的实际工作目录
  ...
  题目_0911.md          人工查看页
production.sqlite3      唯一数据源
```

原始 prompt 只存 SQLite，不复制到工作目录。每道正式题必须有真实仓库和首轮开始前的干净 GitHub 快照。上述出题请求会自动完成初始工程提交、GitHub 建仓和推送、远程可达性验证、40 位 SHA 快照登记以及机械质检；用户不需要创建仓库、执行 Git 命令或填写快照链接。任何一道题未完成这些步骤时，出题流程必须将其保留为 `BLOCKED`，不能报告批次创建完成。

下面的命令只用于维护人员修复或重新登记快照，不是日常用户步骤：

```bash
python3 tools/batch_pipeline.py --db production.sqlite3 set-repo \
  --batch 0911 --number 1 \
  --repo-url https://github.com/org/repo \
  --snapshot https://github.com/org/repo/commit/完整40位SHA
```

新批次的首轮题统一为“0-1 代码生成”，Bug、Feature 等类型只用于后续轮次。首轮题不能是“简单”，User Prompt 必须是一段自然语言，也不能使用 `项目规范.md` 中禁止或饱和的题材。

## 3. 出题质检

在本项目对话里说：

```text
使用 $cc-usr-question-qc 质检批次 0911 的全部题目。
只检查批次内和跨批次是否存在重复题、近似题或换名套模板。
不要读取任何 Claude Code 轨迹、回复或产物。
```

重复性质检通过后，备注固定写“质检通过”，题目会立即变为 `READY`，可以进入批量启动操作，不再需要单独人工批准。质检本身不会启动 Claude Code。只有发现重复时才记录具体证据。prompt 一旦被修改，题目会自动变回 `BLOCKED`，必须重新执行重复性质检。难度、禁题、仓库、快照、验收标准和复现性由出题及机械门禁负责，不属于 `$cc-usr-question-qc` 的职责。

## 4. 批量启动 Claude Code

双击根目录 [批量启动Claude.command](批量启动Claude.command)，然后：

1. 输入批次名，例如 `0911`。
2. 输入题号，例如 `1,3-5`。
3. 核对题目工作目录。
4. 输入 `RUN`。

每道题会打开一个新的 iTerm2 窗口。程序先进入该题目录，再从 SQLite 读取原始 prompt。交给目标模型的命令严格等价于：

```bash
claude "<SQLite 中保存的原始 prompt>"
```

不会添加 `-p`、模型、权限、输出格式、system prompt、agent 或其他 Claude 参数。Claude Code 保持交互模式，并由 Claude Code 自己保存原始会话轨迹；本项目的启动器不会读取、加工或分析轨迹。默认一次最多启动 4 题。

## 5. 记录 SessionID、PromptID 和人工评分

Claude 每完成一轮有效对话，人工先查看该轮过程和产物，并从 Claude Code 会话记录中复制两个原始标识：

- `SessionID`：当前 Claude 会话窗口的 ID，同一题后续轮次保持不变。
- `PromptID`：本轮 user 消息的 ID，每轮都不同。不要用任务 ID、批量运行 ID 或记录 ID 代替。

AI 严禁打开或分析 `~/.claude/projects`、目标模型回复、代码和 diff，因此这两个 ID 不能由 skill 自动查找，必须由人工原样提供。随后在本项目对话中调用下一步 skill：

```text
使用 $cc-usr-delivery-producer 录入批次 0907 第 1 题第 1 轮。
我会提供该轮的 SessionID、PromptID、五项评分、五项描述、其他问题、提交人和完成时间；请原样入库，不要读取 Claude Code 轨迹、回复或产物。
```

也可以直接运行交互式录入：

```bash
python3 .agents/skills/cc-usr-delivery-producer/scripts/collect_record.py \
  --db production.sqlite3 --batch 0911 --question 1 --turn 1
```

程序会逐项询问人工字段，并自动带出首轮 prompt、快照、题型、难度、语言和 Claude Code 版本，不需要重复填写。若任务首轮已经完成，每题只录入一次 `--turn 1`；若继续对话，则按 `--turn 2` 至 `--turn 10` 顺序逐轮录入，同一会话沿用相同 `SessionID`，每轮填写各自的 `PromptID`。网络或工程故障造成的无效轮次不录入，人工输入“继续”则算一轮。

每条记录先以待质检草稿保存。一个批次的全部有效轮次录入完成后，进入下一步。

## 6. 交付质检和 Excel 导出

结构质检：

```bash
python3 .agents/skills/cc-usr-delivery-qc/scripts/validate_records.py \
  --db production.sqlite3 --batch 0911
```

另一位人工质检员核对后批准：

```bash
python3 .agents/skills/cc-usr-delivery-qc/scripts/validate_records.py \
  --db production.sqlite3 --batch 0911 \
  --approve 0911-001-T01 --reviewer 李四 --confirm-human-review YES
```

导出 Excel：

```bash
python3 .agents/skills/cc-usr-excel-exporter/scripts/export_xlsx.py \
  --db production.sqlite3 --batch 0911 \
  --output "outputs/CC_Codex 用户满意度标注（0911）.xlsx"
```

导出器只导出已经通过人工交付质检的记录，严格写入原模板的 A:Y 25 列，不修改任何人工评分和描述。

## 目录说明

```text
项目规范.md                  最高规则
.env                         URL、模型名、Key，仅保存在本机
production.sqlite3           唯一生产数据源
<批次>/qNNN/                 Claude Code 实际工作目录
<批次>/题目_<批次>.md         人工查看页
<批次>/.runs/                不含配置值和 prompt 的启动记录
outputs/                     最终 Excel
```
