# 使用说明

日常只需要按下面七步操作。题目、质检状态、运行登记和 Excel 字段都保存在根目录 `production.sqlite3`。

## 网页生产控制台

macOS 双击根目录 `启动生产控制台.command`，Windows 双击 `启动生产控制台.bat`，浏览器会打开 `http://127.0.0.1:4173`。控制台直接读取 `production.sqlite3`，可以切换批次、查看五个生产阶段、搜索和筛选题目、查看 Prompt、SessionID、逐轮评分及交付文件。

页面可以直接生成新题目批次、打开批次或题目目录、启动已经通过质检的 Claude Code 题目、复检交付记录和导出 Excel；“运行配置”页面还可以直接修改中转 URL、模型、API Key 和提交人。API Key 不会回显，保存时留空表示保留原 Key。

控制台只监听本机地址。网页可以显示 URL、模型名和提交人，但不会返回完整 API Key；只显示是否已配置及末尾提示。关闭启动控制台的终端窗口即可停止服务。

需要让本机持续循环生产时，使用左侧“调度器”。Windows 的任务计划程序安装、启动、停止和卸载方法见 [调度器使用说明.md](调度器使用说明.md#131-windows-本机)。

## 1. 填写一次连接配置

首次使用可以打开根目录 `.env`，填写中转站提供的 URL、模型名和 Key；之后也可以直接在网页的“运行配置”中修改：

```dotenv
CC_SWITCH_BASE_URL=https://你的中转站地址
CC_SWITCH_MODEL=你的模型名
CC_SWITCH_API_KEY=你的Key
CC_USR_SUBMITTER=你的真实姓名
```

不需要再打开 CC Switch 客户端。URL 必须是中转站提供的 Claude/Anthropic 兼容地址。

每次启动题目时，程序都会重新读取前三项，并通过环境变量交给 Claude Code。它们不会作为 Claude 命令行参数，不会写入 SQLite、题目目录或启动记录。`CC_USR_SUBMITTER` 只用于交付表的“提交人”字段，必须填写真实姓名，不能填写工具或模型名称。不要把 `.env` 上传或提交到仓库。

## 2. 创建一批题目

你不需要自己建批次目录。打开网页的“生成题目”，填写批次名、题目数量、业务关键词、技术关键词和补充要求，即可生成并复制标准出题命令，再粘贴到本项目对话中执行。也可以直接在本项目对话里说：

```text
使用 $cc-usr-question-author 创建批次。
批次名：0911
题目数量：10
出题要求：这里写业务方向、技术栈、题型比例、仓库来源等要求。
严格遵守 项目规范.md。创建完成后运行出题机械质检，不要启动目标模型。
```

系统会创建：

```text
0911/
  q001/                 第 1 题的实际工作目录
  q002/                 第 2 题的实际工作目录
  ...
  题目_0911.md          人工查看页
production.sqlite3      唯一数据源
```

原始 prompt 只存 SQLite，不复制到工作目录。每道正式题必须有真实仓库和首轮开始前的干净 GitHub 快照。上述出题请求会自动完成初始工程提交、GitHub 建仓和推送、远程可达性验证、40 位 SHA 快照登记以及机械质检；用户不需要创建仓库、执行 Git 命令或填写快照链接。任何一道题未完成这些步骤时，出题流程必须将其保留为 `BLOCKED`，不能报告批次创建完成。

下面的命令只用于维护人员修复或重新登记快照，不是日常用户步骤：

```bash
python3 tools/batch_pipeline.py --db production.sqlite3 set-repo \
  --batch 0911 --number 1 \
  --repo-url https://github.com/org/repo \
  --snapshot https://github.com/org/repo/commit/完整40位SHA
```

新批次的首轮题统一为“0-1 代码生成”，Bug、Feature 等类型只用于后续轮次。首轮题不能是“简单”，User Prompt 必须是一段自然语言，也不能使用 `项目规范.md` 中禁止或饱和的题材。

## 3. 出题质检

在本项目对话里说：

```text
使用 $cc-usr-question-qc 质检批次 0911 的全部题目。
只检查批次内和跨批次是否存在重复题、近似题或换名套模板。
不要读取任何 Claude Code 轨迹、回复或产物。
```

重复性质检通过后，备注固定写“质检通过”，题目会立即变为 `READY`，可以进入批量启动操作，不再需要单独人工批准。质检本身不会启动 Claude Code。只有发现重复时才记录具体证据。prompt 一旦被修改，题目会自动变回 `BLOCKED`，必须重新执行重复性质检。难度、禁题、仓库、快照、验收标准和复现性由出题及机械门禁负责，不属于 `$cc-usr-question-qc` 的职责。

## 4. 批量启动 Claude Code

macOS 双击根目录 [批量启动Claude.command](批量启动Claude.command)，Windows 双击 `批量启动Claude.bat`，然后：

1. 输入批次名，例如 `0911`。
2. 输入题号，例如 `1,3-5`。
3. 核对题目工作目录。
4. 输入 `RUN`。

启动器的 `auto` 模式在所有平台都使用无交互模式；macOS 检测到 iTerm2 时会打开可见窗口展示 Claude Code 输出，但不会进入交互式工作区信任页。没有 iTerm2 时才使用后台无终端模式。程序先进入该题目录，再从 SQLite 读取原始 prompt，不会等待工作区信任或权限确认。

```bash
claude --print --dangerously-skip-permissions \
  --verbose --output-format stream-json \
  --permission-mode bypassPermissions --permission-prompts none \
  "<SQLite 中保存的原始 prompt>"
```

这些参数会跳过逐条权限询问，并通过非交互模式跳过工作区信任页。URL、模型和 Key 仍通过环境变量传递，不会写入 SQLite、题目目录或启动记录；Claude Code 自己保存原始会话轨迹，本项目启动器不会读取、加工或分析轨迹。可以一次选择整个批次，启动器会为每道题分别创建会话。

无交互模式会使用 Claude Code 的 `--print`、`stream-json`、`--permission-mode bypassPermissions` 和 `--permission-prompts none`，因此可在 iTerm2 可见窗口中运行，同时跳过工作区信任页和权限询问。启动辅助程序会把实时事件转换成可读的模型文本、工具名、文件路径、执行命令和结果摘要；这些终端显示不替代或改写 Claude 原始 JSONL 轨迹。`auto` 会在 macOS+iTerm2 环境中使用这种可见无交互窗口；`--mode server` 使用后台模式；只有显式指定 `--mode iterm` 才会进入原生交互模式，该模式可能显示 Claude Code 自身的工作区信任提示。

Claude Code 按具体题目目录记录信任状态，信任批次父目录并不能可靠覆盖之后新建的 `qNNN` 子目录。默认 `auto` 不依赖已保存的信任记录，而是对每道题都通过无交互模式直接跳过信任页。

## 5. 分析、评分和入库

批次中的 Claude Code 会话全部完成后，在本项目对话中调用：

```text
使用 $cc-usr-delivery-producer 处理批次 0907 的全部已完成题目。请自动定位对应的 Claude Code 会话，提取每轮 SessionID 和 PromptID，读取轨迹、回复、代码、diff 与验证结果，严格按照 项目规范.md 的五维标准逐轮评分并撰写描述，然后写入 production.sqlite3。请用专业程序员的自然语言写清问题出现的位置、实际行为和具体影响，有证据时再说明原因及更合理的处理方式；不要使用固定标签、套话或重复句式，也不要在描述中提及评价者和生成过程。不要修改目标模型的代码或轨迹。
```

该 skill 会自动完成：

1. 根据 SQLite 中的运行时间、题目目录和首轮原始 prompt 定位 `~/.claude/projects` 中的对应会话。
2. 提取会话 `SessionID` 和每轮 user 消息的 `PromptID`，同一会话保持相同 SessionID，每轮 PromptID 不同。
3. 区分有效轮次；纯网络或模型服务故障不计入，思考超限后的人工“继续”计入一轮。
4. 检查每轮过程、最终回复、实际代码变化、测试和运行结果，核对模型声明与真实产物是否一致。
5. 按交付完整性、指令遵循、任务规划、推理能力和执行能力分别给出 1–5 分，并为每一项写具体描述，包括满分项。
6. 将每轮记录写入 SQLite，自动带出首轮 prompt、初始快照、题型、难度、语言和 Claude Code 版本，并把自动产出的记录如实标记为 `human_authored=false`；该审计字段不会写进评分描述。

描述不能只写“任务没完成”“代码有 bug”“过程比较乱”等概括性结论。扣分时要把问题发生的位置、具体行为和需求影响自然地写进一段话，并引用真实文件、函数、报错、命令或缺失功能；过程和产物都有问题时必须分别覆盖。五项描述不能换个维度名后复用同一句话，不能出现英文要素标签、箭头、方括号占位符、自我指代或生成过程说明。skill 只分析和记录，不能替目标模型修改代码，也不能虚构证据。

每条记录先以待质检草稿保存。一个批次的全部有效轮次录入完成后，进入下一步。

## 6. 交付质检

第五步完成并写入全部有效轮次后，在本项目对话中调用：

```text
使用 $cc-usr-delivery-qc 质检批次 0907 的全部交付记录。逐项检查 CC_Codex 用户满意度标注（试标）.xlsx 要求的 27 个提交字段是否符合 项目规范.md，包括字段完整性、SessionID、PromptID、轨迹文件、快照、枚举值、分数与描述一致性、描述质量、时间、父记录、重复和同会话一致性。发现不合规项不要只报告，必须根据 SQLite、对应会话和实际产物修正，记录修正理由并重新质检；不得猜测或编造无法确认的值。全部记录符合规范后将审核备注写为“质检通过”，并明确回复质检通过。不要导出 Excel。
```

该 skill 会先检查整个批次；发现问题时，从题目与运行登记、对应会话、初始快照和实际产物中核实正确值，通过受控修正入口写回，并保存每次修改的理由和前后值。修正后会重新检查全部 27 个导出字段。只有错误和警告都清零，才会给批次中的记录写入独立的交付质检状态，并将“审核备注”固定写为“质检通过”。

质检可以修改交付记录中的不合规字段，但不能修改目标模型的会话、回复和仓库产物，也不能为了通过而编造 SessionID、PromptID、时间、测试结果或评分依据。确实没有证据恢复的字段必须保持未通过，并明确指出缺少什么来源。

## 7. Excel 导出

第六步已经明确显示“质检通过”后，再单独调用：

```text
使用 $cc-usr-excel-exporter 导出批次 0907。只导出已经通过交付质检的完整批次，按照 CC_Codex 用户满意度标注（试标）.xlsx 的 A:AA 27 列生成一个全新的 Excel 文件，同时把每道题对应的原始 JSONL 轨迹复制到 0907 批次根目录。轨迹文件名必须标明批次和题号，Excel 的“轨迹文件”列保持空白，我上传后自行填写。不要包含任何其他 SQLite 内部字段，不要修改评分、描述或其他记录内容。完成后告诉我导出条数、Excel 路径和每个轨迹文件路径。
```

如只交付部分题目，在提示词中明确题号：

```text
使用 $cc-usr-excel-exporter 导出批次 0907 的第 1-2 题。只导出已经通过交付质检的记录，按照 CC_Codex 用户满意度标注（试标）.xlsx 的 A:AA 27 列生成一个全新的 Excel 文件，同时把这两题对应的原始 JSONL 轨迹复制到 0907 批次根目录。轨迹文件名必须标明批次和题号，Excel 的“轨迹文件”列保持空白，我上传后自行填写。不要修改评分、描述或其他记录内容。完成后告诉我导出条数、Excel 路径和每个轨迹文件路径。
```

整批导出时，导出器会在 `0907/` 中生成类似 `CC_Codex 用户满意度标注（0907-第1-2题）.xlsx` 的文件，并把原始轨迹按 `轨迹_0907_第1题_<SessionID>.jsonl`、`轨迹_0907_第2题_<SessionID>.jsonl` 命名后放在同一目录。指定题号时只导出对应题目的全部有效轮次。每次运行都会创建新 Excel；已有同名文件时自动追加编号，不会覆盖旧文件。轨迹也原样复制且不覆盖已有文件。

新 Excel 只有一个“数据表”工作表和模板要求的 27 列，不带示例行；“轨迹文件”列固定留空，等上传后手工填写。除“审核备注”外，不带质检状态、修正历史、内部主键等 SQLite 专用字段。这个 skill 只负责导出和原样复制轨迹，不再质检、分析轨迹或修正记录。

## 一键自动流水线

控制台的“生产批次”页面提供“一键全流程”。它会按以下顺序自动执行：先检查并修复运行环境；由 Codex CLI 并行完成题目机械质检和重复性质检；在 Docker 中以受控并发运行 Claude CLI；由 Codex CLI 并行定位原始 Claude JSONL 轨迹、完成交付生产；再由 Codex CLI 完成交付质检和 Excel 导出。任何阶段失败都会停止后续阶段并把原因写入本地 SQLite，页面刷新后仍可查看。失败或中断后可从最新任务点击“从失败处重试”，系统会保留旧日志和原始轨迹，逐题跳过已有有效结果，从首个失败阶段继续。

模型阶段只把 SQLite 中保存的该题原始 User Prompt 作为 Claude CLI 的 Prompt。中转 URL、模型名和 Key 通过容器环境变量传入，不拼接到 Prompt，也不写入题目目录、运行日志或出题记录。Claude 的原始 JSONL 轨迹直接保存在批次 `.runs/auto-*/claude-home/` 中，交付生产读取原始文件，导出时逐字节复制。

在“运行配置”中设置 Claude Docker 镜像、容器内命令以及三类并发数（范围 1-8，默认 2）。Windows 和 macOS 会在 Docker 引擎不可用时尝试启动 Docker Desktop；使用默认的 `claude-cli:latest` 时，镜像缺失或仍以 root 运行会根据仓库内 Dockerfile 自动重建。首次执行仍只接受尚无模型运行记录的新批次，已有失败任务必须通过其重试入口继续，避免覆盖有效轨迹。

自动流水线列表对每个批次只显示最新一次执行，完整历史仍保存在 SQLite。模型跑题期间每 15 秒检查一次容器状态和轨迹更新时间，前端每 6 秒读取一次状态，页面位于后台时暂停请求。轨迹连续 10 分钟没有更新时显示需关注状态；连续 30 分钟没有更新则判定为停滞，停止容器、保留已有轨迹并将该题置为可重试。接口只返回日志末尾 30 KB，完整日志不会从数据库中删除。

## 目录说明

```text
项目规范.md                  最高规则
.env                         URL、模型名、Key、真实提交人，仅保存在本机
production.sqlite3           唯一生产数据源
<批次>/qNNN/                 Claude Code 实际工作目录
<批次>/题目_<批次>.md         人工查看页
<批次>/.runs/                不含配置值和 prompt 的启动记录
<批次>/CC_Codex*.xlsx        每次新建的最终 Excel
<批次>/轨迹_*.jsonl          按题号命名的原始轨迹副本
```
