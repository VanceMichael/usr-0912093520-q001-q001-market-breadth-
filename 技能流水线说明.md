# Coding Agent 用户满意度生产流水线

`production.sqlite3` 是唯一数据源。批次 Markdown 只用于人工查看，题目工作目录只放交给 Claude Code 操作的仓库内容。

```text
批次名 + 数量 + 出题要求
  -> 自动创建批次目录、N 个工作目录、1 个题目 MD
  -> 自动提交初始工程、推送 GitHub、验证并登记 40 位 SHA 快照
  -> 出题机械质检 + 重复性质检（通过即 READY）
  -> 双击启动器，选择批次和题号
  -> 自动提取 SessionID、PromptID，分析轨迹和产物，逐轮评分并入库
  -> 交付字段质检，修正后记录“质检通过”
  -> 单独生成只含 27 个必需字段的新 Excel，并导出按题号命名的原始轨迹
```

六个 skill 的职责：

1. `$cc-usr-question-author`：按规范设计批次题目，创建目录、Markdown 和 SQLite 记录，并自动完成每题初始工程提交、GitHub 推送、远程验证和快照登记；用户无需参与这些操作。
2. `$cc-usr-question-qc`：只检查批次内及跨批次的重复题、近似题和换名套模板；通过后题目立即可由批量启动器选择，但不会自动启动。
3. `$cc-usr-claude-runner`：读取根目录 `.env`，按批次和题号在各自目录中启动 Claude Code；自动判断 Mac+iTerm2 或服务器后台模式，也可显式覆盖；只传 SQLite 原始 prompt，不读取轨迹。
4. `$cc-usr-delivery-producer`：作为 Claude 跑题后的下一步，自动定位并读取对应轨迹和产物，提取 SessionID 与每轮 PromptID，按规范完成五维评分和证据描述并写入 SQLite；不得修改目标模型产物或编造证据。
5. `$cc-usr-delivery-qc`：检查全部必需交付字段，对有证据支持的不合规值进行审计式修正，复检无误后记录“质检通过”；不导出 Excel。
6. `$cc-usr-excel-exporter`：按整批或指定题号读取已通过交付质检的记录，在批次根目录生成仅含 A:AA 27 列且“轨迹文件”列留空的新工作簿，同时原样复制按批次和题号命名的 JSONL 轨迹；每次生成新文件，不再质检或修改记录。

评分入库负责轨迹与产物分析、五维评分和描述撰写。自动产出的记录必须在内部审计字段中如实标记，所有判断必须有可核验证据，导出的描述只保留自然、具体的工程判断。交付质检和 Excel 导出是后续两个独立步骤。
