# Coding Agent 用户满意度生产流水线

`production.sqlite3` 是唯一数据源。批次 Markdown 只用于人工查看，题目工作目录只放交给 Claude Code 操作的仓库内容。

```text
批次名 + 数量 + 出题要求
  -> 自动创建批次目录、N 个工作目录、1 个题目 MD
  -> 自动提交初始工程、推送 GitHub、验证并登记 40 位 SHA 快照
  -> 出题机械质检 + 重复性质检（通过即 READY）
  -> 双击启动器，选择批次和题号
  -> 人工逐轮评分并入库
  -> 结构质检 + 人工交付批准
  -> 按批次导出 25 列 Excel
```

六个 skill 的职责：

1. `$cc-usr-question-author`：按规范设计批次题目，创建目录、Markdown 和 SQLite 记录，并自动完成每题初始工程提交、GitHub 推送、远程验证和快照登记；用户无需参与这些操作。
2. `$cc-usr-question-qc`：只检查批次内及跨批次的重复题、近似题和换名套模板；通过后题目立即可由批量启动器选择，但不会自动启动。
3. `$cc-usr-claude-runner`：读取根目录 `.env`，按批次和题号在各自目录中通过 iTerm2 启动 Claude Code；只传 SQLite 原始 prompt，不加其他 Claude 参数，也不读取轨迹。
4. `$cc-usr-delivery-producer`：把人工填写的逐轮评分和描述原样写入 SQLite。
5. `$cc-usr-delivery-qc`：做结构检查并记录人工质检决定。
6. `$cc-usr-excel-exporter`：把已通过人工质检的记录写入原始 25 列模板。

AI 可以出题和质检题目，但严禁分析目标模型的轨迹、回复和产物，也严禁代写或润色评分描述。
