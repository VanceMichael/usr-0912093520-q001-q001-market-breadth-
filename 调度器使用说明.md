# 常驻生产调度器使用说明

本文说明如何使用项目中的常驻生产调度器，在本机或一台/多台 VPS 上持续完成以下流程：

```text
抓取新闻主题
  -> Codex 出题并执行出题质检
  -> Claude Code 在 Docker 中跑题
  -> Codex 生成交付记录
  -> Codex 执行交付质检
  -> 导出 Excel 和原始 JSONL
  -> 等待下一轮并继续生产
```

本文只介绍“调度器”模式。调度器支持“完整流水线”和“只出题+质检”两种模式。页面中的手动出题和“一键自动流水线”仍然可用，但不能与同一台机器上的常驻调度器同时执行。

## 1. 调度器解决什么问题

调度器由 `tools/pipeline_daemon.py` 提供，是一个可长期驻留、状态可恢复的生产进程。它会定期抓取新闻主题，优先接管数据库里尚未结束的批次，没有活跃批次时再创建新批次，并持续运行直到被暂停或停止。

调度器适合以下场景：

- VPS 需要全天无人值守持续生产。
- 本机开机期间需要自动循环生产。
- 多台机器使用各自的新闻来源、模型配置和 GitHub 账号独立生产。
- 需要在一个页面查看每台机器的队列、资源、容器、批次进度和日志。
- 单题失败时希望保留其他合格题，尽可能生成可交付结果。

调度器不会跨机器分配任务，也不会对多台机器抓取的新闻做全局去重。当前设计是每台机器拥有独立的 `production.sqlite3`、新闻来源、批次目录、模型凭据和 GitHub Token。为了避免多节点出重复题，应给不同节点配置不同的新闻来源。

## 2. 各组件的职责

### 2.1 Codex CLI

Codex 是生产控制 agent，负责：

- 根据新闻主题调用 `$cc-usr-question-author` 创建题目和初始工程。
- 创建并推送题目 GitHub 仓库，登记可访问的 40 位 SHA 初始快照。
- 调用 `$cc-usr-question-qc` 执行机械质检、重复性和反模板质检。
- 读取 Claude 原始轨迹、代码变化和验证结果，生成交付评分记录。
- 执行交付质检并修正可由证据确认的不合规字段。
- 调用导出器生成 Excel，并逐字节复制原始 JSONL。

Codex 使用自己在宿主机上的 CLI 登录和模型配置。运行配置中的 `CC_SWITCH_MODEL` 不控制 Codex 底层模型。

### 2.2 Claude Code CLI

Claude Code 是目标模型，只负责在题目工作目录中完成题目。每道题在独立的短生命周期 Docker 容器中运行，默认限制如下：

- 每个容器最多使用 1 CPU。
- 每个容器最多使用 2 GB 内存。
- 每个容器的进程数上限为 512。
- 题目目录挂载到容器 `/workspace`。
- 每次尝试使用独立的 Claude 状态目录和 HOME，避免重试读取上一次失败状态。
- 中转 URL、API Key 和模型名通过容器环境变量传入，不拼接到题目 Prompt。

Claude Code 使用 `.env` 中的 `CC_SWITCH_BASE_URL`、`CC_SWITCH_API_KEY` 和 `CC_SWITCH_MODEL`。

### 2.3 生产控制台

控制台由 `webapp/server.py` 提供。它读取本机数据库，也可以通过配置的远程控制台地址代理访问 VPS。控制台提供：

- 调度器开始、暂停、排空、停止、重启和错误重试。
- 新闻队列、活跃批次和各阶段计数。
- CPU、内存、磁盘、Git 分支和提交状态。
- 当前 Docker 容器、最近生产周期、结构化事件和原始日志。
- 新闻来源、难度比例、每批题数、并发和模型凭据配置。
- 本机或 VPS 的 Excel/JSONL 交付包下载。

## 3. 一个生产周期如何运行

### 3.1 抓取新闻

每个周期首先读取当前节点启用的新闻来源，将新文章转换为新闻主题并写入 `news_topics`。相同主题不会在同一个数据库里重复插入。

新闻只是业务背景种子。出题不得复制新闻标题、正文或网站代码，也不能把未经验证的新闻事实变成实现要求。

### 3.2 接管已有批次

调度器优先处理状态不是 `completed`、`partial` 或 `failed` 的已有批次。这样进程重启后不会跳过尚未完成的工作，也不会为已经终结的批次重复交付。

如果同一台机器上存在正在运行的手动出题任务或手动一键流水线，自动调度器本轮不接单，等待手动任务结束后再继续。

### 3.3 自动出题和出题质检

没有活跃批次且待跑题数量低于水位线时，调度器会按“每批题数”一次领取等量的不同新闻主题，并创建名称类似下面的新批次：

```text
0909143025
```

Codex 必须在同一次出题任务里完成：

1. 按题号将新闻主题与题目一一绑定，每条新闻只生成一道纯后端首轮 0-1 代码生成题。
2. 创建每题的真实初始工程。
3. 提交并推送到当前 VPS 对应的 GitHub 账号。
4. 登记 GitHub 仓库地址和 40 位 SHA 快照链接。
5. 执行机械质检。
6. 执行批次内和跨批次的重复、自然度与反模板质检。
7. 对模板化 Prompt 先修复、重新质检，再标记为可运行。

当前自动出题只允许 Go、Python、Node.js（JavaScript/TypeScript）和 Java 纯后端项目，不生成前端或全栈题。

可用的新主题少于“每批题数”时，调度器不会复用已有主题或只创建半个批次，而是等待后续周期抓取到足够的不同新闻。

调度器会按固定结构组装给 Codex 的出题指令，批次名自动生成为 `MMDDHHMMSS` 形式的 10 位纯数字，批次目录与批次名保持一致。题目数量、难度分配和新闻来源取当前节点配置，本轮主题清单取已经抓取并领取的不同新闻。示例：

```text
使用 $cc-usr-question-author 创建批次。
批次名：0910010203
题目数量：10
难度分配：中等 5 道（50%）、困难 5 道（50%）
出题要求：业务关键词：从 https://www.chinanews.com/scroll-news/news1.html 读取主题来进行出题，本轮必须按下方新闻主题清单的 question_no 将主题与题目一一绑定，每条新闻只生成一道题；技术关键词：需要 Docker；补充要求：在整批中合理覆盖 Node.js、Python、Go、Java，每道题只选择一种主要后端技术栈，并且只生成纯后台服务。
新闻主题清单：[{"question_no": 1, "title": "...", "article_url": "...", "source_url": "..."}]
严格遵守 项目规范.md。完成真实初始工程、GitHub 可访问快照和登记后，运行出题机械质检，并使用 $cc-usr-question-qc 完成重复、自然度和反模板质检；不要启动目标模型。
```

如果启用了多个新闻来源，“业务关键词”会按本批次实际领取的主题列出对应来源 URL，并去除重复 URL。每个题目仍只对应主题清单中的一条新闻。Docker 是每题硬要求；四类语言是在整批中保持合理覆盖，而不是要求一道题同时使用四种语言。

### 3.4 模型与交付流水线并行

完整模式使用跨批次全局队列，不再把一个批次作为必须串行完成的调度边界。后台持续维护已通过出题质检的题目缓冲；全局模型池从所有非终态批次领取题目，全局交付池随时领取已经完成模型运行的题目，单线程导出池则逐个结束已经满足终态条件的批次。

```text
Codex 出题缓冲 -> 批次 A/B/C 的质检通过题目
                       | 全局 Claude 模型池
批次 A 题 1 完成 ------+-> 立即进入全局 Codex 交付池
批次 B/C 其他题目 --------> 继续并行运行 Claude
批次 A 达到终态 ----------> 独立进入导出池，不等待 B/C
```

`CC_PIPELINE_READY_TARGET` 是全节点的已质检题目缓冲目标；低于目标时，出题生产器可以在模型和交付运行期间继续创建新批次。`CC_NEWS_READY_TARGET` 是待用新闻水位，默认 40；低于水位时会重新读取已启用的新闻来源。抓取后仍不足一个完整批次时，调度器会保留现有新闻并定时重试，不会空转创建不完整批次。`CC_PIPELINE_MODEL_CONCURRENCY` 控制同时运行的 Claude Docker 容器数；`CC_PIPELINE_CODEX_CONCURRENCY` 控制同时处理的题目交付数。对同一道题，模型、交付生产、交付质检和导出仍保持严格先后顺序。Codex 出题、交付质检和 Excel/JSONL 导出步骤保持不变，没有为了节省 Codex 调用而跳过任何质量环节。

模型和交付领取使用数据库题目级租约。同一题在租约有效期内不会被另一个 worker 重复领取；调度器重启时会回收遗留租约，再按数据库真实状态继续。租约只负责防止重复执行，不替代题目阶段和质检状态判断。

模型并发是配置上限，调度器还会根据当前可用内存和一分钟系统负载决定是否继续领取新题。可用内存低于 20% 时降低放行量，低于 12% 时暂停领取新模型题目；已经运行的容器不会被资源调节器强制终止。资源恢复后会自动补足到配置上限。

### 3.5 重试与工作区恢复

Claude 任务失败或超时后，下一次尝试前必须验证题目登记的初始快照，然后执行：

- 将 Git HEAD 恢复到登记的 `local_initial_sha`。
- 删除上次尝试产生的未跟踪文件和构建产物。
- 验证 Git 工作区没有未提交变更，且 HEAD 与 GitHub 快照 SHA 一致。
- 创建新的 Claude 状态和 HOME 目录后重新运行。

失败分为两套独立预算。模型实现、运行超时或有效轨迹不合格默认最多尝试 2 次；429、500、502、503、504、网关超时、连接重置等临时中转故障默认最多尝试 3 次，且不占用模型实现次数。401/403、无效 API Key、不支持的模型和 Docker 环境错误不会盲目重试。初始快照无效、工作区不能恢复或相应预算耗尽时，该题标记为 `blocked`。

网关重试采用带随机抖动的递增退避，默认约为 30-45 秒、90-135 秒，且不超过 300 秒。同一节点在 120 秒内出现 2 次临时网关故障时会打开节点级熔断器，暂停启动新的 worker 180 秒；已运行任务不会被主动终止。冷却后只放行一个探测任务，成功即恢复，继续失败则再次冷却。

出题失败与整机故障分开处理。Codex 出题超时、空批次或出题门禁未通过时，新闻主题会释放回待用池，出题生产器按 30、60、120、240、480、最高 900 秒退避重试；全局模型池和交付池继续消化已有 READY 题目，不会因为单个空批次自动暂停整台机器。页面会把这类情况显示为 `authoring` 可恢复故障。

调度器被“立即停止”或逻辑重启时，会终止当前子进程和 Docker 容器，将数据库里的孤儿运行记录标记为 `interrupted`，并把对应题目放回可恢复状态。人为中断本身不消耗模型失败重试次数，但恢复后仍会先清理工作区。

### 3.6 部分交付

批次不再要求所有题都成功才允许导出：

- 全部题目合格并导出：批次状态为 `completed`。
- 一部分题目永久失败，其余题目完整通过交付质检并导出：状态为 `partial`。
- 没有任何可交付题目：状态为 `failed`。

导出器只选择已经完成模型运行、生成完整交付记录且交付质检通过的题目。失败题不会混入 Excel 或 JSONL，也不会阻止好题交付。

### 3.7 只出题+质检模式

在“调度器”页面点击“只出题+质检”后，调度器会持续执行新闻抓取、Codex 出题、创建初始仓库、机械质检和重复/反模板质检。每个周期创建并质检一个新批次，但不会启动 Claude Code、生成交付记录、执行交付质检或导出文件。

只有题目数量正确、全部初始快照完整、机械质检通过、重复/反模板质检通过且 Prompt 指纹一致时，批次才标记为 `ready`。Codex 命令即使以退出码 0 结束，只要实际结果没有满足这些门禁，本周期仍会记为失败。

已经出好且质检通过的批次会保留在本机数据库和批次目录中。之后点击“开始”会切换回完整流水线，调度器优先处理这些 `ready` 批次，再继续创建新批次。

从完整模式切换为只出题模式时，当前正在运行的完整周期不会被强制终止，新模式从下一周期生效。如果必须立刻停止 Claude 或 Codex，先使用“立即停止”，再点击“只出题+质检”。

## 4. 首次运行前的要求

每个节点都需要准备：

- Python 3.9 或更高版本。
- Git 和 GitHub CLI `gh`。
- Docker；macOS 使用 Docker Desktop。
- 宿主机可非交互运行的 Codex CLI。
- Docker worker 镜像中的 Claude Code CLI，以及 Go、Python、Node.js 和 Java 21/JDK 运行环境。
- 可访问 Claude/Anthropic 兼容接口的 URL、模型名和 API Key。
- 对目标 GitHub 账号具有建仓和推送权限的 Token。
- 足够的磁盘空间；默认剩余空间低于 15 GB 时自动暂停。

VPS 初次准备示例：

```bash
gh repo clone MonsterRHD/UserSatisfactionRating UserSatisfactionRating-codex
cd UserSatisfactionRating-codex
git switch codex/vps-docker-pipeline
cp .env.example .env
docker build -f Dockerfile.worker -t ccusr-claude-worker:local .
```

通用 worker 镜像通过 `amazoncorretto:21` 多阶段复制 Java 21 JDK，因此 Java 题可以在容器内使用 `java` 和 `javac` 完成构建、测试与验收；不需要在宿主机单独安装 Java。

验证宿主机 Codex 和容器内 Claude：

```bash
codex --version
docker run --rm ccusr-claude-worker:local claude --version
```

不要把真实 Token 或 API Key 提交到 Git。`.env` 只保存在当前节点。

## 5. 运行配置

推荐通过当前节点控制台左侧“运行配置”修改。保存后的配置写入该节点根目录 `.env`，从下一次相关任务开始生效。密钥输入框留空表示保留原值。

页面保存配置时，“中转 URL”“模型名称”“API Key”和“提交人”是表单必填项。这是为了保证同一份配置随时可以切回完整流水线。实际运行时，两种调度模式的最低要求不同：

- “只出题+质检”必须具备宿主机 Codex 登录、GitHub 建仓权限、至少一个启用的新闻来源，以及有效的难度比例和每批题数。它不会使用 Claude 中转配置、Claude Docker 镜像或提交人。
- “完整流水线”需要上面全部出题条件，还必须配置 Claude 中转 URL、模型、API Key、真实提交人和可用的 Claude worker 镜像。

| 页面字段 | 环境变量 | 是否必须 | 用途与配置规则 |
| --- | --- | --- | --- |
| 中转 URL | `CC_SWITCH_BASE_URL` | 完整流水线必须；页面保存必须 | Claude Code 使用的兼容接口根地址，填写完整 HTTPS URL，不带账号、查询参数和 `#` 片段 |
| 模型名称 | `CC_SWITCH_MODEL` | 完整流水线必须；页面保存必须 | 中转服务实际支持的 Claude 模型标识，不是 Codex 的底层模型 |
| API Key | `CC_SWITCH_API_KEY` | 完整流水线必须；首次保存必须 | Claude 接口凭据；以后留空表示保留 `.env` 中的原值 |
| 提交人 | `CC_USR_SUBMITTER` | 完整流水线必须；页面保存必须 | Excel “提交人”列使用的真实姓名，不填工具名或模型名 |
| GitHub / gh Token | `CC_GITHUB_TOKEN` | 自动出题必须 | 注入宿主机 Codex/`gh`，用于创建并推送题目仓库；每台 VPS 使用自己的 Token，以后留空表示保留原值 |
| 难度比例 | `CC_AUTHOR_DIFFICULTY_WEIGHTS` | 自动出题必须 | 可启用中等、困难、地狱，多项比例合计必须为 100；默认中等 100% |
| 每批题数 | `CC_AUTHOR_BATCH_SIZE` | 自动出题必须 | 每批 1-20 道，默认 10 道 |
| 模型运行方式 | `CC_PIPELINE_MODEL_MODE` | 手动流水线可选 | `local` 或 `docker`；常驻调度器始终使用隔离 Docker worker |
| Docker 镜像 | `CC_CLAUDE_DOCKER_IMAGE` | 完整调度器必须可用 | Claude worker 镜像名，推荐 `ccusr-claude-worker:local` |
| 容器内命令 | `CC_CLAUDE_DOCKER_COMMAND` | 可选 | 镜像内 Claude Code 命令，默认 `claude` |
| 质检并发 | `CC_PIPELINE_QC_CONCURRENCY` | 可选 | 1-32，默认 2；主要控制手动一键流水线的出题质检并发 |
| 模型并发 | `CC_PIPELINE_MODEL_CONCURRENCY` | 可选 | 1-32，默认 2；全局 Claude 模型池上限，实际放行量会根据资源压力降低 |
| 交付并发 | `CC_PIPELINE_CODEX_CONCURRENCY` | 可选 | 1-32，默认 2；全局 Codex 交付生产与质检池上限 |
| 已质检题目缓冲 | `CC_PIPELINE_READY_TARGET` | 可选 | 1-200，默认 40；低于该数量时后台继续创建并质检新批次 |
| 待用新闻水位 | `CC_NEWS_READY_TARGET` | 可选 | 不低于单批题数，默认 40；低于该数量时重新抓取新闻来源 |
| 单 worker CPU | `CC_CLAUDE_WORKER_CPUS` | 可选 | 默认 1；每个 Claude Docker 容器的 CPU 配额，页面允许 0.25-4 |
| 单 worker 内存 | `CC_CLAUDE_WORKER_MEMORY` | 可选 | 默认 `2g`；每个 Claude Docker 容器的内存上限，例如 `1536m`、`2g` |
| 网关最大尝试 | `CC_GATEWAY_MAX_ATTEMPTS` | 可选 | 临时网关故障的独立尝试上限，默认 3；不占用模型实现次数 |
| 退避初始秒数 | `CC_GATEWAY_BACKOFF_BASE` | 可选 | 第一次网关重试的基础等待，默认 30 秒，并自动加入随机抖动 |
| 退避上限秒数 | `CC_GATEWAY_BACKOFF_MAX` | 可选 | 单次退避上限，默认 300 秒，不能小于初始秒数 |
| 熔断失败阈值 | `CC_GATEWAY_CIRCUIT_THRESHOLD` | 可选 | 统计窗口内达到该临时故障数即暂停新 worker，默认 2 |
| 熔断统计窗口 | `CC_GATEWAY_CIRCUIT_WINDOW` | 可选 | 临时网关故障的滚动统计窗口，默认 120 秒 |
| 熔断冷却时间 | `CC_GATEWAY_CIRCUIT_COOLDOWN` | 可选 | 熔断后暂停新 worker 的时间，默认 180 秒 |
| SOLO2 地址 | `CC_SOLO2_ORIGIN` | 平台提交时必须 | 默认 `https://solo2.jzxhnh.com`；填写完整 HTTP/HTTPS 地址，不含账号、查询参数和片段 |
| 质检后自动提交 | `CC_SOLO2_AUTO_SUBMIT` | 可选 | 默认 `false`，即人工确认；设为 `true` 后才会自动提交全部交付质检通过的记录 |
| SOLO2 提交并发 | `CC_SOLO2_CONCURRENCY` | 可选 | 1-8，默认 1；使用独立线程池，不占 Claude 或 Codex worker 名额 |
| SOLO2 最大尝试 | `CC_SOLO2_MAX_ATTEMPTS` | 可选 | 1-10，默认 3；仅自动提交受此上限约束，人工确认可在排障或重新登录后再次提交 |
| 新闻来源 | 数据库 `news_feeds` | 自动出题必须 | 至少启用 1 个可访问的 HTTP/HTTPS 新闻列表页；不同节点配置不同来源可避免跨节点重复 |

注意：常驻调度器的自动出题和出题质检由一个 Codex 控制任务完成，因此 `CC_PIPELINE_QC_CONCURRENCY` 目前主要影响手动“一键自动流水线”，不改变常驻调度器的出题 agent 数量。

难度比例可以多选，但启用项的比例合计必须等于 100%。例如：

```dotenv
CC_AUTHOR_DIFFICULTY_WEIGHTS='{"中等":50,"困难":30,"地狱":20}'
CC_AUTHOR_BATCH_SIZE=10
```

2 核 / 8 GB VPS 的完整示例（值均为占位符，真实密钥只写入 VPS 的 `.env`）：

```dotenv
CC_SWITCH_BASE_URL=https://relay.example.com
CC_SWITCH_MODEL=claude-model-name
CC_SWITCH_API_KEY=replace-with-claude-api-key
CC_USR_SUBMITTER=your-real-name
CC_GITHUB_TOKEN=replace-with-github-token

CC_AUTHOR_DIFFICULTY=中等
CC_AUTHOR_DIFFICULTY_WEIGHTS='{"中等":50,"困难":30,"地狱":20}'
CC_AUTHOR_BATCH_SIZE=10

CC_CLAUDE_DOCKER_IMAGE=ccusr-claude-worker:local
CC_CLAUDE_DOCKER_COMMAND=claude
CC_PIPELINE_MODEL_MODE=docker
CC_PIPELINE_QC_CONCURRENCY=2
CC_PIPELINE_MODEL_CONCURRENCY=2
CC_PIPELINE_CODEX_CONCURRENCY=1
CC_PIPELINE_READY_TARGET=40
CC_NEWS_READY_TARGET=40
CC_CLAUDE_WORKER_CPUS=1
CC_CLAUDE_WORKER_MEMORY=2g
CC_GATEWAY_MAX_ATTEMPTS=3
CC_GATEWAY_BACKOFF_BASE=30
CC_GATEWAY_BACKOFF_MAX=300
CC_GATEWAY_CIRCUIT_THRESHOLD=2
CC_GATEWAY_CIRCUIT_WINDOW=120
CC_GATEWAY_CIRCUIT_COOLDOWN=180
CC_SOLO2_ORIGIN=https://solo2.jzxhnh.com
CC_SOLO2_AUTO_SUBMIT=false
CC_SOLO2_CONCURRENCY=1
CC_SOLO2_MAX_ATTEMPTS=3
```

保存后在“运行环境”点击“检测并修复”，至少确认 Codex、Git/`gh`、Docker 和所选 Claude 运行环境均可用。新闻来源不写在 `.env`，在“运行配置”的“新闻出题来源”区域逐条添加、启用并保存。

## 6. 在控制台中日常操作

### 6.1 打开页面

本机默认地址：

```text
http://127.0.0.1:4173/
```

VPS 控制台默认只监听 VPS 的 `127.0.0.1:8787`。建立 SSH 隧道后，从本机打开映射端口，例如：

```bash
ssh -N -L 18787:127.0.0.1:8787 ubuntu@VPS_IP
```

输入 SSH 密码并回车后，终端持续没有输出是正常现象，说明隧道正在前台保持。此时打开：

```text
http://127.0.0.1:18787/
```

如果要直接打开 `http://VPS_IP:18787/`，只能在已配置云安全组和系统防火墙白名单的 VPS 上使用。将控制台 service 的启动参数改为 `--host 0.0.0.0 --port 18787 --allow-public`，并只允许固定管理 IP 访问 18787；程序默认仍拒绝公网绑定，缺少 `--allow-public` 会启动失败。

关闭运行 SSH 命令的终端或按 `Ctrl+C` 会断开隧道。

### 6.2 选择节点

打开左侧“调度器”，页面顶部会同时显示本机和所有已启用 VPS。选择节点后，页面中的控制按钮、队列、资源、批次和日志都指向当前节点。

选择远程 VPS 后发送“开始”或“停止”，请求会由本机控制台代理到该 VPS，不会误操作本机调度器。

### 6.3 控制按钮含义

| 按钮 | 行为 | 是否终止当前任务 | 适用场景 |
| --- | --- | --- | --- |
| 开始 | 将目标状态设为运行，开始新周期 | 否 | 正常开始生产 |
| 只出题+质检 | 持续创建题目、初始仓库并完成出题质检 | 否 | 提前储备已质检题目 |
| 暂停接单 | 当前周期结束后不再开始下一周期 | 否 | 临时停止新增生产 |
| 排空后暂停 | 明确等待当前周期结束，再切换为暂停 | 否 | 计划维护或调整配置 |
| 立即停止 | 终止当前 Codex/Claude 子进程和任务容器 | 是 | 紧急停机、发现配置错误 |
| 逻辑重启 | 终止当前子任务、回收状态并重新开始 | 是 | 任务卡死或配置变更后重跑 |
| 清除错误并重试 | 清空连续失败计数并恢复运行 | 视当前状态 | 修复外部故障后重新尝试 |

优先使用“排空后暂停”进行常规维护。只有当前任务不应继续时才使用“立即停止”。

### 6.4 服务运行和调度状态不是一回事

VPS 上 `ccusr-pipeline.service` 显示 `active (running)`，只表示守护进程在线。页面显示“已停止”时，守护进程仍会驻留并持续更新心跳，但不会抓取新闻、创建批次或运行题目。

这个设计使页面可以随时发送新的“开始”指令，不需要先登录 VPS 启动 Python 进程。

页面状态行会显示当前运行模式：“完整流水线”或“只出题+质检”。运行模式保存在 `production.sqlite3`，暂停、停止和服务重启不会丢失；点击“开始”会明确切回完整流水线。

## 7. 页面信息如何理解

### 7.1 调度器状态

- `running`：正在运行生产周期。
- `paused`：已暂停，不会开始新周期。
- `draining`：正在等待当前周期结束，然后暂停。
- `stopped`：停止接单；守护进程可以仍然在线。
- `restarting`：正在中断当前子任务并执行逻辑重启。
- `error`：本轮失败或触发了安全保护。

页面同时展示“目标状态”和实际心跳。心跳超过约 15 秒会被视为守护进程离线，此时应先检查 systemd/launchd 服务，而不是反复点击开始。

Windows 节点则检查任务计划程序中的 `CCUSR Console` 和 `CCUSR Scheduler`，或在 PowerShell 执行 `Get-ScheduledTask -TaskName 'CCUSR *'`；不要只根据 Python 进程是否存在判断业务状态。

### 7.2 阶段

- `news`：抓取并登记新闻主题。
- `author`：Codex 出题、建仓和出题质检。
- `model`：Claude 模型池和 Codex 交付池正在流水处理。
- `delivery`：交付记录生产。
- `delivery_qc`：交付质检。
- `export`：Excel 和 JSONL 导出。
- `idle`：等待控制指令或下一轮。
- `error`：当前周期失败。

优化后的常驻流程会让模型和交付重叠运行，所以总览阶段可能仍显示 `model`，但批次进度里的“模型、记录、质检”三个计数会分别增长。逐题细节以原始日志为准。

### 7.3 批次进度

- “模型 X/Y”表示已经完成 Claude 运行的题目数。
- “出题质检完成”表示批次处于 `ready`，尚未启动 Claude。
- “记录 X/Y”表示已经生成至少一条交付记录的题目数。
- “质检 X/Y”表示交付记录已经通过质检的题目数。
- “部分交付”表示合格题已经导出，但批次里有永久失败题。
- “无可交付题目”表示该批次没有任何满足导出门槛的题。

### 7.4 Claude 运行现场

“Claude 运行现场”按当前所选节点列出所有状态为 `running` 的模型题目。点击左侧题目后，右侧终端窗口会持续显示 Claude 的文本增量、工具调用、工具结果和会话结束信息。向下箭头用于开启或关闭自动跟随，下载按钮会把当前浏览器中已经加载的输出保存为文本日志。

新启动的 worker 使用 Claude Code 的 `stream-json` 输出，每道题写入独立日志；页面每秒按字节游标增量读取，不会反复下载完整文件。升级前已经启动的旧 worker 没有流式日志时，服务会回退读取该次运行登记的 Claude JSONL 轨迹。切换本机、VPS 或运行题目时，页面会重置游标，远程输出仍通过本机控制台的 VPS 代理读取。

每个 Docker worker 都使用 `--safe-mode --disable-slash-commands` 启动 Claude Code，禁用项目 Skill、Slash Command、插件、Hook、MCP、Memory 和自定义指令加载。容器仅挂载当前题目目录到 `/workspace`，另挂载该次运行专属的 Claude 状态目录和 HOME；不同题目、不同重试不会复用容器或状态目录。交付质检按记录的 SessionID 精确关联成功 run，检查原始轨迹中的 PromptID、工具调用/结果配对、最终回复，并拒绝读取题目工作区外的 Skill、`CLAUDE.md`、`AGENTS.md` 或 Claude/Codex 配置。

调度器每 5 秒更新一次运行心跳，同时观察实时日志和 Claude 状态目录。默认 300 秒内完全没有输出视为启动失败，已经开始后连续 900 秒没有任何输出或轨迹变化视为运行停滞，`CC_PIPELINE_WORKER_TIMEOUT`（或编排器的 `--timeout`）仍作为整次运行的硬超时。以上失败都会强制回收当前容器；下一次尝试先把题目仓库恢复到登记的初始提交，再创建全新容器。

进程退出码为 0 还不是有效完成的充分条件。模型阶段的“有效轨迹门禁”必须找到唯一一份与本题原始 Prompt 精确匹配且带 SessionID/PromptID 的主 JSONL，会话文件名必须与 SessionID 一致，文件中不能混入其他 SessionID；Claude 内部 `<task-notification>`、meta 事件和压缩摘要不算用户轮次。主轨迹还必须包含真实（非 `<synthetic>`）assistant 响应、至少一次完整配对且顺序正确的工具调用和结果、工具调用之后的最终文字回复，未访问工作区外的指令或配置，并且题目工作区相对登记的初始快照确实有代码文件变化。任一条件不满足，运行记录标记为 `failed`、题目退回 `approved`，不会进入交付生产。

交付生产后的描述门禁会按五个维度分别核对证据。满分描述必须写出具体核验动作和客观结果；非满分描述必须指出可定位的文件、模块、命令、测试或接口证据，并说明造成的工程后果。规划、推理和执行描述还需分别体现拆解或状态追踪、错误前提或遗漏分支、具体工具动作及后续复验。批量待交付记录之间若重复较长片段，也会被拦截，要求改写为针对本题事实的自然中文。

可在 `.env` 中按需调整监督阈值，通常不需要修改：

```dotenv
CC_CLAUDE_HEARTBEAT_SECONDS=5
CC_CLAUDE_START_TIMEOUT=300
CC_CLAUDE_STALLED_TIMEOUT=900
CC_PIPELINE_WORKER_TIMEOUT=3600
CC_GATEWAY_MAX_ATTEMPTS=3
CC_GATEWAY_BACKOFF_BASE=30
CC_GATEWAY_BACKOFF_MAX=300
CC_GATEWAY_CIRCUIT_THRESHOLD=2
CC_GATEWAY_CIRCUIT_WINDOW=120
CC_GATEWAY_CIRCUIT_COOLDOWN=180
```

四项都必须是正整数。启动超时应大于镜像冷启动和首次模型响应的正常耗时；停滞超时应覆盖一次较长的构建或测试，避免把仍在运行但暂时没有输出的任务误杀。

该功能在 macOS、Windows 和 VPS Linux 上使用同一套 Python 二进制文件读取与 HTTP JSON 接口，不依赖 `tail`、PTY、iTerm2、PowerShell 窗口或 systemd 日志格式。日志路径只能来自数据库登记且必须位于项目 `runs` 目录下；API Key、GitHub Token 等常见凭据会在返回浏览器前脱敏。它是只读观察窗口，不会向正在运行的 Claude 会话发送输入，也不会改变容器生命周期。

### 7.5 中断后的“继续”轮次

Claude 因思考次数或上下文限制中断后，可以在同一个隔离会话中输入“继续”。该输入算一个新的有效轮次，但 SOLO2 的交付字段沿用被中断任务的完整 User Prompt 和 PromptID。SQLite 另外保存本次真实输入“继续”、它自己的 PromptID 和连续继续次数，确保质检仍能精确定位原始事件。

交付生产会逐轮写入记录。质检允许被“继续”紧接的中断轮次没有最终文字回复，但恢复轮次必须包含工具调用结果和最终文字回复；`其他问题` 必须说明发生中断并通过继续恢复，多次继续还要写明累计次数。Excel 和 SOLO2 只使用正式交付字段，内部原始字段不会扩展 28 列模板。

### 7.6 单题人工接管

题目模型运行处于运行中、失败、超时或中断状态时，题目行会显示终端按钮。点击后系统会：

1. 为该题加维护锁，使模型池和交付池不再领取它。
2. 停止该题仍在运行的旧 Docker 容器。
3. 复用该次运行专属的题目工作区、Claude 私有状态和 HOME。
4. 生成并复制一条带 `claude --resume <SessionID>` 的独立交互容器命令。

在 macOS/Linux 终端或 Windows PowerShell 粘贴并执行命令，即可直接观察 Claude 原生交互界面并输入“继续”或具体修正要求。接管容器仍只挂载当前题目目录、该题 Claude 状态和该题 HOME，读取不到调度器项目根目录与其中的 Skills。

完成后回到页面点击“完成接管”。系统只有在同一 Session 新增了真实用户轮次，且有效轨迹门禁确认工具调用闭环、最终回复和代码变化后，才解除维护锁并把题目交回交付流水线。没有执行接管命令、没有新增轮次或轨迹仍不完整时，按钮会返回具体错误，题目继续保持维护状态。

### 7.7 完整重置题目

完整重置用于放弃一道题的全部本地执行结果并从初始快照重新开始。页面会二次确认，确认后系统先停止该题容器，再执行 `git reset --hard <初始 SHA>` 和 `git clean -ffdx`，随后删除：

- 该题所有模型运行记录和每次运行专属的 Claude 状态、HOME、日志目录。
- 该题所有评分与交付质检记录。
- 该题尚未成功的 SOLO2 本地提交状态。
- 批次根目录中可能包含旧数据的 Excel 和已复制轨迹文件。

题目会回到 `approved`，重置次数加一，并在 `question_reset_audit` 留下重置原因、删除记录数和时间。初始快照无法验证、容器无法停止或题目已有 SOLO2 成功提交时，系统拒绝继续删除；SOLO2 远端提交不可由本地重置撤销。

### 7.8 SOLO2 平台提交

SOLO2 默认为人工确认提交。打开“导出中心”，先用平台账号登录，再检查当前批次的可提交数和历史状态，点击“确认提交当前批次”或题目行的发送按钮。平台密码不会写入 `.env` 或数据库，登录 Cookie 仅保存在当前节点 `.local-auth/solo2.cookies`；macOS/Linux 权限为 0600，Windows 使用当前用户 ACL。

提交前仍要求记录已经通过本地交付质检。系统读取平台当前动态字段定义，把本地 28 字段映射后上传原始 JSONL，并按 `record_id` 保证幂等：已成功的记录再次操作只会跳过，不会重复创建。Excel/JSONL 的本地导出和 ZIP 下载保持独立，不会因 SOLO2 登录或网络故障而停止。

需要无人值守提交时，在“运行配置”打开“质检后自动提交”并保存。自动提交使用独立线程池：401/403 进入认证阻塞，重新登录后人工重试；字段或 schema 不兼容进入结构阻塞，修正平台或本地映射后人工重试；429、5xx、504 和网络连接问题进入退避等待，在最大尝试次数内自动恢复。调度器进程意外退出时，超过五分钟的遗留“提交中”租约允许重新领取。

## 8. 多 VPS 管理

### 8.1 为每台 VPS 建立独立隧道

每个节点使用不同本地端口：

```bash
ssh -N -L 18787:127.0.0.1:8787 ubuntu@VPS_1_IP
ssh -N -L 18788:127.0.0.1:8787 ubuntu@VPS_2_IP
ssh -N -L 18789:127.0.0.1:8787 ubuntu@VPS_3_IP
```

这三个命令需要分别保持运行。建议为 VPS 配置 SSH Key，避免隧道重连时重复输入密码。

### 8.2 在页面登记节点

打开本机控制台左侧“VPS 管理”，为每台 VPS 填写：

- 节点名称，例如 `VPS-01`。
- 控制台地址，例如 `http://127.0.0.1:18787`。
- SSH 隧道命令，仅作为操作提示保存。
- 是否启用。

保存后，“VPS 管理”可以查看远程批次和下载交付包；“调度器”可以在本机和各 VPS 之间切换，查看完整生产现场并发送控制指令。

### 8.3 修改某台 VPS 的运行配置

本机控制台的“运行配置”只修改本机 `.env`。要修改 VPS 的新闻来源、Token、难度、批次大小或并发，请在“VPS 管理”中点击“打开原始页面”，进入该 VPS 的隧道地址，再打开它自己的“运行配置”。

每台 VPS 应使用自己的 `CC_GITHUB_TOKEN` 和 GitHub 账号。Token 必须有当前项目和题目仓库所需权限，但不要写进节点 URL、SSH 命令、Git Remote 或日志。

## 9. 并发配置建议

在“运行配置”的并发区域点击“读取本机配置”，页面会读取宿主机和 Docker 的 CPU、内存，并给出“推荐配置”和“最大性能配置”。点击对应的“应用”只会填充表单，仍需点击“保存运行配置”才会写入 `.env`。

macOS 和 Windows 使用 Docker Desktop 时，宿主机资源不等于 Docker 虚拟机可用资源。检测结果会优先采用 `docker info` 返回的 CPU 和内存配额；要使用更多资源，先在 Docker Desktop 的 Resources 设置中调整，再重新检测。Linux VPS 通常直接使用宿主机配额。

并发目标是最大化最终通过质检的交付物，而不是让 CPU 始终达到 100%。Claude 容器、Codex 交付、Docker、SQLite 和操作系统需要共享资源。自动推荐基于通用资源模型，是起始值而不是吞吐保证；中转接口限流、仓库大小和题目复杂度仍会影响实际结果。

| 机器有效配置 | 档位 | 质检并发 | 模型并发 | 交付并发 | 每批题数 | 题目缓冲 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 2 核 / 8 GB | 推荐 | 2 | 2 | 1 | 10 | 40 |
| 4 核 / 16 GB | 推荐 | 2 | 3 | 2 | 10 | 40 |
| 8 核 / 32 GB | 推荐 | 2 | 8 | 4 | 16 | 40 |
| 8 核 / 32 GB | 最大性能 | 2 | 10 | 5 | 20 | 40 |
| 16 核 / 32 GB | 推荐 | 2 | 12 | 6 | 20 | 40 |
| 16 核 / 32 GB | 最大性能 | 2 | 14 | 6 | 20 | 40 |

计划采购的 16 核 / 32 GB VPS 建议先使用 `2 / 12 / 6`，单 worker `1 CPU / 2g`，每批 20 道、缓冲 40 道。确认运行稳定后，再应用最大性能档 `2 / 14 / 6`。最大性能档仍保留约 4 GB 内存和 2 个 CPU 逻辑核给操作系统、Docker、SQLite、Codex 与导出流程，并由运行时自适应放行做最后保护。

运行至少 24 小时后，同时满足下面条件才保留最大性能档：

- CPU 长期低于 75%-80%。
- 内存长期低于 70%。
- 磁盘 I/O 没有明显等待。
- Claude 接口没有明显 429、超时或连接错误。
- Codex 交付队列没有持续扩大；如持续扩大，应优先提高交付并发或排查单题交付耗时。
- 最终交付通过率没有下降。

不要把自动推荐理解为可以无上限超卖 CPU。模型并发超过有效 CPU 数后，容器、交付进程和 Docker 管理会争抢调度时间；自适应控制只能停止接新题，不会杀掉已经启动的容器，因此过高的配置仍可能造成一段时间的资源拥塞。

## 10. 日志和可观测性

### 10.1 页面日志

“调度器”页面提供两类日志：

- “事件”：结构化记录周期开始、主题领取、阶段切换、停止、重启、安全保护和失败。
- “原始日志”：读取 `runs/daemon/` 中最近日志的末尾内容。

可以按级别、阶段和关键词筛选，也可以下载当前筛选结果。远程节点日志同样通过本机控制台代理读取。

### 10.2 文件位置

```text
runs/daemon/author-<批次>.log        Codex 出题和出题质检日志
runs/daemon/workers-global.log       跨批次模型、交付和导出池总日志
runs/daemon/workers-<批次>.log       手动或兼容模式的单批次流水线日志
runs/<批次>/<任务ID>/docker-*.log    单次 Claude 容器输出
runs/<批次>/<任务ID>/<run-id>/       单次尝试的 Claude 状态和 HOME
runs/<批次>/<任务ID>/delivery-*.log  逐题交付生产/质检日志
runs/<批次>/export.log               批次导出日志
```

VPS systemd 日志：

```bash
journalctl -u ccusr-pipeline.service -f
journalctl -u ccusr-console.service -f
```

调度器默认清理 `runs/daemon/` 中超过 30 天的日志，并把该目录的日志总量控制在约 2 GB。题目工作目录、原始轨迹和交付产物不属于这项日志清理范围，需要单独规划磁盘归档。

## 11. 下载 Excel 和 JSONL

### 11.1 本机批次

打开“导出中心”，选择已经完成或部分交付的批次，点击“下载交付包”。浏览器会下载：

```text
ccusr-delivery-<批次>.zip
```

ZIP 中只包含批次根目录已经登记的：

- `CC_Codex*.xlsx`
- `轨迹_*.jsonl`

每次成功下载都会在数据库记录下载次数和最近下载时间，页面会显示“已下载”标识。

### 11.2 VPS 批次

打开本机“VPS 管理”，选择在线节点和批次，点击“下载交付包”。远程控制台在 VPS 内存中生成 ZIP，本机控制台代理下载，不需要使用 `scp` 手动查找文件。

如果节点离线，先确认 SSH 隧道仍在运行，再确认 VPS 的 `ccusr-console.service` 正常。

## 12. 安全保护和自动恢复

### 12.1 连续失败熔断

自动熔断只针对会让整机继续运行也无法成功的系统性故障：SQLite 不可用、401/403 或无效 API Key、模型配置错误、Docker/运行环境错误。默认同一类系统性故障连续 3 个周期后，调度器进入错误/暂停状态，不再领取新任务。Codex 出题超时、新闻源暂时失败、单题模型失败、轨迹门禁失败或导出失败会进入降级/退避状态，但不会触发整机暂停。

页面状态区会显示 `failure_kind`、数据库状态、最近成功时间、自动熔断原因和熔断时间。修复系统性故障后点击“清除错误并重试”，该操作会清空连续失败计数和旧熔断原因；不要只重启 systemd/launchd，因为数据库会保留暂停目标状态。

### 12.2 磁盘保护

默认可用磁盘低于 15 GB 时自动暂停，并在页面显示错误。清理或扩容后再重试。不要只删除 `production.sqlite3`；它是批次、题目、运行、质检和调度状态的唯一数据源。

### 12.3 SQLite 并发

数据库已启用 WAL、30 秒 busy timeout 和外键检查，以支持模型池与交付池并发写入。每个生产周期开始前还会执行 `PRAGMA quick_check(1)` 和一次短写事务；读取损坏、目录不可写或无法获得写连接时标记为 `database_unavailable`，停止领取新任务并保留已有数据，数据库恢复后再次检查。数据库 schema 15 包含题目模型/交付租约、人工维护锁、继续轮次原始字段、SOLO2 提交状态和重置审计；旧数据库在首次启动新版控制台或调度器时自动迁移，已有记录和已提交 SOLO2 的远端编号会保留。仍应避免手工脚本长时间占用写事务，也不要在共享文件系统上让多台机器同时打开同一个 SQLite 文件。

### 12.4 中断恢复

调度器启动时会：

- 把未结束的旧周期标记为 `interrupted`。
- 回收数据库中仍标记为运行的 Claude 容器。
- 清理上次进程留下的模型和交付题目租约。
- 将对应题目放回可恢复状态。
- 在下一次模型重试前恢复 Git 初始快照。

不要手工把失败题直接改成 `completed`，否则交付阶段可能读取到不完整轨迹或脏工作区。

## 13. 本机常驻运行

macOS 可以安装 launchd 服务：

```bash
chmod +x deploy/install_macos_services.sh
./deploy/install_macos_services.sh
```

安装后：

- 控制台默认监听 `127.0.0.1:4173`。
- 调度器和控制台会在登录时启动，并在异常退出后重新拉起。
- 电脑关机、休眠或 Docker Desktop 停止期间，本机不会生产。
- 本机新闻来源和 VPS 新闻来源互不影响。

安装服务不等于必须立即生产。打开页面确认配置后，再从“调度器”点击“开始”。

### 13.1 Windows 本机

Windows 使用任务计划程序常驻控制台和调度器，不依赖 systemd 或 launchd。需要先安装：

- Python 3.9 或更高版本，并确保 `python.exe` 在 PATH 中。
- Docker Desktop，并确认 `docker info` 成功。
- Git、GitHub CLI `gh`，以及已经登录的 Codex CLI。
- Claude worker 镜像 `ccusr-claude-worker:local`。可以在 PowerShell 中构建：

```powershell
docker build -f Dockerfile.worker -t ccusr-claude-worker:local .
```

在项目根目录用 PowerShell 注册两个登录后自动启动的任务：

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\deploy\install_windows_scheduler.ps1
```

脚本创建：

- `CCUSR Console`：启动 `webapp/server.py`，监听 `127.0.0.1:4173`。
- `CCUSR Scheduler`：启动 `tools/pipeline_daemon.py --loop`，默认模型并发 2、Codex 交付并发 1。

安装后打开 [http://127.0.0.1:4173/](http://127.0.0.1:4173/)，先在“运行配置”填写 `.env` 所需值，再到“调度器”确认心跳和目标状态。新数据库默认目标状态可能是 `running`；如果暂时不生产，请立即点击“停止”，确认状态行显示 `stopped`。任务计划程序在线只代表进程常驻，不代表业务一定在跑。

卸载两个任务：

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\deploy\install_windows_scheduler.ps1 -Remove
```

不想注册登录任务时，也可以手动启动 `启动生产控制台.bat`；调度器首次由页面发送“开始”或“只出题+质检”时会自动后台启动。Windows 下控制台会优先尝试运行 `CCUSR Scheduler` 任务，未注册时才直接创建后台 Python 进程。

Windows 停止或重启调度器时，程序使用 `taskkill /T` 回收 Codex、Docker 编排器及其子进程，不依赖 Unix 进程组信号。调度器锁使用 Windows 文件锁，重复启动第二个调度器会直接退出并在日志中提示已有实例。

## 14. VPS systemd 管理

安装或更新服务文件：

```bash
sudo install -m 0644 deploy/systemd/ccusr-pipeline.service /etc/systemd/system/ccusr-pipeline.service
sudo install -m 0644 deploy/systemd/ccusr-console.service /etc/systemd/system/ccusr-console.service
sudo systemctl daemon-reload
sudo systemctl enable --now ccusr-pipeline.service ccusr-console.service
```

全新数据库中的调度器目标状态默认为 `running`，因此首次执行 `enable --now` 可能立即开始生产。如果希望先检查配置，可以只启动控制台，在“调度器”页面发送“立即停止”，确认目标状态为 `stopped` 后再启动 `ccusr-pipeline.service`：

```bash
sudo systemctl enable --now ccusr-console.service
# 通过控制台把调度器设为 stopped
sudo systemctl enable --now ccusr-pipeline.service
```

检查状态：

```bash
systemctl status ccusr-pipeline.service --no-pager
systemctl status ccusr-console.service --no-pager
curl -I http://127.0.0.1:8787/
```

仓库内默认 systemd 文件使用：

```text
用户：ubuntu
项目目录：/home/ubuntu/UserSatisfactionRating-codex
控制台：127.0.0.1:8787
```

如果 VPS 用户或项目目录不同，必须先修改两个 service 文件中的 `User`、`Group`、`WorkingDirectory` 和 `ExecStart`。

## 15. 安全部署更新流程

更新代码前，不要在调度器运行中直接 `git pull`。推荐操作顺序：

1. 页面点击“排空后暂停”，等待当前周期结束。
2. 确认页面没有运行中的 Docker 容器。
3. 停止调度器和控制台服务。
4. 快进拉取指定生产分支。
5. 如 service 文件变化，重新安装并执行 `daemon-reload`。
6. 重新构建有变化的 Docker worker 镜像。
7. 启动两个服务。
8. 检查 HTTP、Git 提交、调度器心跳和数据库状态。
9. 确认无误后在页面点击“开始”。

命令示例：

```bash
cd /home/ubuntu/UserSatisfactionRating-codex
sudo systemctl stop ccusr-console.service ccusr-pipeline.service
git pull --ff-only origin codex/vps-docker-pipeline
docker build -f Dockerfile.worker -t ccusr-claude-worker:local .
sudo install -m 0644 deploy/systemd/ccusr-pipeline.service /etc/systemd/system/ccusr-pipeline.service
sudo install -m 0644 deploy/systemd/ccusr-console.service /etc/systemd/system/ccusr-console.service
sudo systemctl daemon-reload
sudo systemctl start ccusr-pipeline.service ccusr-console.service
```

已有数据库在服务重启后会保留其中的 `stopped` 或 `paused` 目标状态。此时 `systemctl start` 不代表一定开始业务生产，最终以“调度器”页面显示的目标状态为准。

## 16. 常见问题

### SSH 输入密码后没有反应

`ssh -N -L ...` 只建立隧道，不打开远程 shell，也不会持续输出日志。密码正确后终端停在那里就是正常状态。保持窗口不关闭，并访问映射后的本机端口。

### 页面显示节点离线

依次检查：

1. SSH 隧道终端是否仍在运行。
2. 节点控制台地址和本地映射端口是否一致。
3. `ccusr-console.service` 是否为 active。
4. VPS 本机执行 `curl http://127.0.0.1:8787/api/scheduler` 是否成功。
5. 防火墙或 SSH 是否断线。

### 服务 active，但没有继续出题

检查调度器页面的目标状态。常见原因是：

- 调度器处于 `stopped` 或 `paused`。
- 同机手动任务仍显示为 queued/running。
- 系统性故障达到熔断阈值；页面会显示具体 `failure_kind` 和熔断原因。
- 出题生产器正在退避；此时已有 READY 题仍会继续跑，日志中可查看下一次重试等待时间。
- 剩余磁盘低于安全阈值。
- 新闻来源全部失败且数据库没有待用主题。
- 存在尚未终结的旧批次，调度器正在优先恢复它。

### 为什么进度长期显示“模型阶段”

优化后的常驻调度器会让 Claude 模型池与 Codex 交付池重叠运行，`model` 是这一段流水线的总阶段。查看“模型、记录、质检”三个计数，或切换到原始日志判断具体正在处理哪一步。

### 为什么有题失败，批次仍然导出了

这是预期行为。永久失败题标记为 `blocked`，通过交付质检的题正常导出，批次标记为 `partial`。ZIP 中不会包含失败题。

### 修改配置后是否需要重启服务

题数、难度和新闻来源会在出题生产器后续创建批次时重新读取。模型并发、交付并发、题目缓冲和 worker 资源限制由当前全局流水线子进程在启动时读取，不会改变已经运行的容器。为了确保新的并发配置完整生效，建议先“排空后暂停”，保存配置，再点击“开始”；不需要重启控制台或操作系统服务。

## 17. 日常检查清单

每天建议检查一次：

- 调度器心跳正常，连续失败数为 0。
- 新闻待用数量不是长期为 0。
- 模型完成、交付记录、质检通过三个计数都在增长。
- 没有容器长时间停留且日志不更新。
- CPU、内存和磁盘没有长期超过阈值。
- Git 分支和提交是预期的生产版本，工作区没有未知修改。
- `partial` 和 `failed` 批次有明确失败原因。
- 已完成批次的 Excel 和 JSONL 能正常下载。
- 下载后的 ZIP 已转移到正式交付或归档位置。

每次升级、修改 Token、切换模型或提高并发后，至少观察一个完整批次，再决定是否继续扩大负载。
