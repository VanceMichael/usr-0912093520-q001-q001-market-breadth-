# 常驻生产调度器使用说明

本文说明如何使用项目中的常驻生产调度器，在本机或一台/多台 VPS 上持续完成以下流程：

```text
抓取新闻主题
  -> Codex 出题并执行出题质检
  -> Claude Code 在 Docker 中跑题
  -> Codex 生成交付记录
  -> Codex 执行交付质检
  -> 导出 Excel 和原始 JSONL
  -> 等待下一轮并继续生产
```

本文只介绍“调度器”模式。调度器支持“完整流水线”和“只出题+质检”两种模式。页面中的手动出题和“一键自动流水线”仍然可用，但不能与同一台机器上的常驻调度器同时执行。

## 1. 调度器解决什么问题

调度器由 `tools/pipeline_daemon.py` 提供，是一个可长期驻留、状态可恢复的生产进程。它会定期抓取新闻主题，优先接管数据库里尚未结束的批次，没有活跃批次时再创建新批次，并持续运行直到被暂停或停止。

调度器适合以下场景：

- VPS 需要全天无人值守持续生产。
- 本机开机期间需要自动循环生产。
- 多台机器使用各自的新闻来源、模型配置和 GitHub 账号独立生产。
- 需要在一个页面查看每台机器的队列、资源、容器、批次进度和日志。
- 单题失败时希望保留其他合格题，尽可能生成可交付结果。

调度器不会跨机器分配任务，也不会对多台机器抓取的新闻做全局去重。当前设计是每台机器拥有独立的 `production.sqlite3`、新闻来源、批次目录、模型凭据和 GitHub Token。为了避免多节点出重复题，应给不同节点配置不同的新闻来源。

## 2. 各组件的职责

### 2.1 Codex CLI

Codex 是生产控制 agent，负责：

- 根据新闻主题调用 `$cc-usr-question-author` 创建题目和初始工程。
- 创建并推送题目 GitHub 仓库，登记可访问的 40 位 SHA 初始快照。
- 调用 `$cc-usr-question-qc` 执行机械质检、重复性和反模板质检。
- 读取 Claude 原始轨迹、代码变化和验证结果，生成交付评分记录。
- 执行交付质检并修正可由证据确认的不合规字段。
- 调用导出器生成 Excel，并逐字节复制原始 JSONL。

Codex 使用自己在宿主机上的 CLI 登录和模型配置。运行配置中的 `CC_SWITCH_MODEL` 不控制 Codex 底层模型。

### 2.2 Claude Code CLI

Claude Code 是目标模型，只负责在题目工作目录中完成题目。每道题在独立的短生命周期 Docker 容器中运行，默认限制如下：

- 每个容器最多使用 1 CPU。
- 每个容器最多使用 2 GB 内存。
- 每个容器的进程数上限为 512。
- 题目目录挂载到容器 `/workspace`。
- 每次尝试使用独立的 Claude 状态目录和 HOME，避免重试读取上一次失败状态。
- 中转 URL、API Key 和模型名通过容器环境变量传入，不拼接到题目 Prompt。

Claude Code 使用 `.env` 中的 `CC_SWITCH_BASE_URL`、`CC_SWITCH_API_KEY` 和 `CC_SWITCH_MODEL`。

### 2.3 生产控制台

控制台由 `webapp/server.py` 提供。它读取本机数据库，也可以通过配置的远程控制台地址代理访问 VPS。控制台提供：

- 调度器开始、暂停、排空、停止、重启和错误重试。
- 新闻队列、活跃批次和各阶段计数。
- CPU、内存、磁盘、Git 分支和提交状态。
- 当前 Docker 容器、最近生产周期、结构化事件和原始日志。
- 新闻来源、难度比例、每批题数、并发和模型凭据配置。
- 本机或 VPS 的 Excel/JSONL 交付包下载。

## 3. 一个生产周期如何运行

### 3.1 抓取新闻

每个周期首先读取当前节点启用的新闻来源，将新文章转换为新闻主题并写入 `news_topics`。相同主题不会在同一个数据库里重复插入。

新闻只是业务背景种子。出题不得复制新闻标题、正文或网站代码，也不能把未经验证的新闻事实变成实现要求。

### 3.2 接管已有批次

调度器优先处理状态不是 `completed`、`partial` 或 `failed` 的已有批次。这样进程重启后不会跳过尚未完成的工作，也不会为已经终结的批次重复交付。

如果同一台机器上存在正在运行的手动出题任务或手动一键流水线，自动调度器本轮不接单，等待手动任务结束后再继续。

### 3.3 自动出题和出题质检

没有活跃批次且待跑题数量低于水位线时，调度器领取一个新闻主题，并创建名称类似下面的新批次：

```text
news0909143025
```

Codex 必须在同一次出题任务里完成：

1. 生成指定数量的纯后端首轮 0-1 代码生成题。
2. 创建每题的真实初始工程。
3. 提交并推送到当前 VPS 对应的 GitHub 账号。
4. 登记 GitHub 仓库地址和 40 位 SHA 快照链接。
5. 执行机械质检。
6. 执行批次内和跨批次的重复、自然度与反模板质检。
7. 对模板化 Prompt 先修复、重新质检，再标记为可运行。

当前自动出题只允许 Go、Python、Node.js（JavaScript/TypeScript）和 Java 纯后端项目，不生成前端或全栈题。

### 3.4 模型与交付流水线并行

通过质检的题目进入 Claude 模型池。模型池不是“整个批次跑完再交付”，而是采用逐题流水方式：

```text
Claude 题 1 完成 -> 立即进入 Codex 交付池
Claude 题 2/3/4 -> 仍可继续并行运行
Codex 对题 1 -> 交付生产 -> 交付质检
```

`CC_PIPELINE_MODEL_CONCURRENCY` 控制同时运行的 Claude Docker 容器数；`CC_PIPELINE_CODEX_CONCURRENCY` 控制同时处理的题目交付数。对同一道题，交付生产和交付质检仍保持严格先后顺序。

### 3.5 重试与工作区恢复

Claude 任务失败或超时后，下一次尝试前必须验证题目登记的初始快照，然后执行：

- 将 Git HEAD 恢复到登记的 `local_initial_sha`。
- 删除上次尝试产生的未跟踪文件和构建产物。
- 验证 Git 工作区没有未提交变更，且 HEAD 与 GitHub 快照 SHA 一致。
- 创建新的 Claude 状态和 HOME 目录后重新运行。

默认最多尝试 2 次。初始快照无效、工作区不能恢复或重试耗尽时，该题标记为 `blocked`，不会继续消耗模型资源。

调度器被“立即停止”或逻辑重启时，会终止当前子进程和 Docker 容器，将数据库里的孤儿运行记录标记为 `interrupted`，并把对应题目放回可恢复状态。人为中断本身不消耗模型失败重试次数，但恢复后仍会先清理工作区。

### 3.6 部分交付

批次不再要求所有题都成功才允许导出：

- 全部题目合格并导出：批次状态为 `completed`。
- 一部分题目永久失败，其余题目完整通过交付质检并导出：状态为 `partial`。
- 没有任何可交付题目：状态为 `failed`。

导出器只选择已经完成模型运行、生成完整交付记录且交付质检通过的题目。失败题不会混入 Excel 或 JSONL，也不会阻止好题交付。

### 3.7 只出题+质检模式

在“调度器”页面点击“只出题+质检”后，调度器会持续执行新闻抓取、Codex 出题、创建初始仓库、机械质检和重复/反模板质检。每个周期创建并质检一个新批次，但不会启动 Claude Code、生成交付记录、执行交付质检或导出文件。

只有题目数量正确、全部初始快照完整、机械质检通过、重复/反模板质检通过且 Prompt 指纹一致时，批次才标记为 `ready`。Codex 命令即使以退出码 0 结束，只要实际结果没有满足这些门禁，本周期仍会记为失败。

已经出好且质检通过的批次会保留在本机数据库和批次目录中。之后点击“开始”会切换回完整流水线，调度器优先处理这些 `ready` 批次，再继续创建新批次。

从完整模式切换为只出题模式时，当前正在运行的完整周期不会被强制终止，新模式从下一周期生效。如果必须立刻停止 Claude 或 Codex，先使用“立即停止”，再点击“只出题+质检”。

## 4. 首次运行前的要求

每个节点都需要准备：

- Python 3。
- Git 和 GitHub CLI `gh`。
- Docker；macOS 使用 Docker Desktop。
- 宿主机可非交互运行的 Codex CLI。
- Docker worker 镜像中的 Claude Code CLI。
- 可访问 Claude/Anthropic 兼容接口的 URL、模型名和 API Key。
- 对目标 GitHub 账号具有建仓和推送权限的 Token。
- 足够的磁盘空间；默认剩余空间低于 15 GB 时自动暂停。

VPS 初次准备示例：

```bash
gh repo clone MonsterRHD/UserSatisfactionRating UserSatisfactionRating-codex
cd UserSatisfactionRating-codex
git switch codex/vps-docker-pipeline
cp .env.example .env
docker build -f Dockerfile.worker -t ccusr-claude-worker:local .
```

验证宿主机 Codex 和容器内 Claude：

```bash
codex --version
docker run --rm ccusr-claude-worker:local claude --version
```

不要把真实 Token 或 API Key 提交到 Git。`.env` 只保存在当前节点。

## 5. 运行配置

推荐通过当前节点控制台左侧“运行配置”修改。保存后的配置写入该节点根目录 `.env`，从下一次相关任务开始生效。密钥输入框留空表示保留原值。

页面保存配置时，“中转 URL”“模型名称”“API Key”和“提交人”是表单必填项。这是为了保证同一份配置随时可以切回完整流水线。实际运行时，两种调度模式的最低要求不同：

- “只出题+质检”必须具备宿主机 Codex 登录、GitHub 建仓权限、至少一个启用的新闻来源，以及有效的难度比例和每批题数。它不会使用 Claude 中转配置、Claude Docker 镜像或提交人。
- “完整流水线”需要上面全部出题条件，还必须配置 Claude 中转 URL、模型、API Key、真实提交人和可用的 Claude worker 镜像。

| 页面字段 | 环境变量 | 是否必须 | 用途与配置规则 |
| --- | --- | --- | --- |
| 中转 URL | `CC_SWITCH_BASE_URL` | 完整流水线必须；页面保存必须 | Claude Code 使用的兼容接口根地址，填写完整 HTTPS URL，不带账号、查询参数和 `#` 片段 |
| 模型名称 | `CC_SWITCH_MODEL` | 完整流水线必须；页面保存必须 | 中转服务实际支持的 Claude 模型标识，不是 Codex 的底层模型 |
| API Key | `CC_SWITCH_API_KEY` | 完整流水线必须；首次保存必须 | Claude 接口凭据；以后留空表示保留 `.env` 中的原值 |
| 提交人 | `CC_USR_SUBMITTER` | 完整流水线必须；页面保存必须 | Excel “提交人”列使用的真实姓名，不填工具名或模型名 |
| GitHub / gh Token | `CC_GITHUB_TOKEN` | 自动出题必须 | 注入宿主机 Codex/`gh`，用于创建并推送题目仓库；每台 VPS 使用自己的 Token，以后留空表示保留原值 |
| 难度比例 | `CC_AUTHOR_DIFFICULTY_WEIGHTS` | 自动出题必须 | 可启用中等、困难、地狱，多项比例合计必须为 100；默认中等 100% |
| 每批题数 | `CC_AUTHOR_BATCH_SIZE` | 自动出题必须 | 每批 1-20 道，默认 10 道 |
| 模型运行方式 | `CC_PIPELINE_MODEL_MODE` | 手动流水线可选 | `local` 或 `docker`；常驻调度器始终使用隔离 Docker worker |
| Docker 镜像 | `CC_CLAUDE_DOCKER_IMAGE` | 完整调度器必须可用 | Claude worker 镜像名，推荐 `ccusr-claude-worker:local` |
| 容器内命令 | `CC_CLAUDE_DOCKER_COMMAND` | 可选 | 镜像内 Claude Code 命令，默认 `claude` |
| 质检并发 | `CC_PIPELINE_QC_CONCURRENCY` | 可选 | 1-8，默认 2；主要控制手动一键流水线的出题质检并发 |
| 模型并发 | `CC_PIPELINE_MODEL_CONCURRENCY` | 可选 | 1-8，默认 2；控制同时运行的 Claude 任务数 |
| 交付并发 | `CC_PIPELINE_CODEX_CONCURRENCY` | 可选 | 1-8，默认 2；控制同时执行的 Codex 交付任务数 |
| 新闻来源 | 数据库 `news_feeds` | 自动出题必须 | 至少启用 1 个可访问的 HTTP/HTTPS 新闻列表页；不同节点配置不同来源可避免跨节点重复 |

注意：常驻调度器的自动出题和出题质检由一个 Codex 控制任务完成，因此 `CC_PIPELINE_QC_CONCURRENCY` 目前主要影响手动“一键自动流水线”，不改变常驻调度器的出题 agent 数量。

难度比例可以多选，但启用项的比例合计必须等于 100%。例如：

```dotenv
CC_AUTHOR_DIFFICULTY_WEIGHTS='{"中等":50,"困难":30,"地狱":20}'
CC_AUTHOR_BATCH_SIZE=10
```

2 核 / 8 GB VPS 的完整示例（值均为占位符，真实密钥只写入 VPS 的 `.env`）：

```dotenv
CC_SWITCH_BASE_URL=https://relay.example.com
CC_SWITCH_MODEL=claude-model-name
CC_SWITCH_API_KEY=replace-with-claude-api-key
CC_USR_SUBMITTER=your-real-name
CC_GITHUB_TOKEN=replace-with-github-token

CC_AUTHOR_DIFFICULTY=中等
CC_AUTHOR_DIFFICULTY_WEIGHTS='{"中等":50,"困难":30,"地狱":20}'
CC_AUTHOR_BATCH_SIZE=10

CC_CLAUDE_DOCKER_IMAGE=ccusr-claude-worker:local
CC_CLAUDE_DOCKER_COMMAND=claude
CC_PIPELINE_MODEL_MODE=docker
CC_PIPELINE_QC_CONCURRENCY=2
CC_PIPELINE_MODEL_CONCURRENCY=2
CC_PIPELINE_CODEX_CONCURRENCY=1
```

保存后在“运行环境”点击“检测并修复”，至少确认 Codex、Git/`gh`、Docker 和所选 Claude 运行环境均可用。新闻来源不写在 `.env`，在“运行配置”的“新闻出题来源”区域逐条添加、启用并保存。

## 6. 在控制台中日常操作

### 6.1 打开页面

本机默认地址：

```text
http://127.0.0.1:4173/
```

VPS 控制台默认只监听 VPS 的 `127.0.0.1:8787`。建立 SSH 隧道后，从本机打开映射端口，例如：

```bash
ssh -N -L 18787:127.0.0.1:8787 ubuntu@VPS_IP
```

输入 SSH 密码并回车后，终端持续没有输出是正常现象，说明隧道正在前台保持。此时打开：

```text
http://127.0.0.1:18787/
```

关闭运行 SSH 命令的终端或按 `Ctrl+C` 会断开隧道。

### 6.2 选择节点

打开左侧“调度器”，页面顶部会同时显示本机和所有已启用 VPS。选择节点后，页面中的控制按钮、队列、资源、批次和日志都指向当前节点。

选择远程 VPS 后发送“开始”或“停止”，请求会由本机控制台代理到该 VPS，不会误操作本机调度器。

### 6.3 控制按钮含义

| 按钮 | 行为 | 是否终止当前任务 | 适用场景 |
| --- | --- | --- | --- |
| 开始 | 将目标状态设为运行，开始新周期 | 否 | 正常开始生产 |
| 只出题+质检 | 持续创建题目、初始仓库并完成出题质检 | 否 | 提前储备已质检题目 |
| 暂停接单 | 当前周期结束后不再开始下一周期 | 否 | 临时停止新增生产 |
| 排空后暂停 | 明确等待当前周期结束，再切换为暂停 | 否 | 计划维护或调整配置 |
| 立即停止 | 终止当前 Codex/Claude 子进程和任务容器 | 是 | 紧急停机、发现配置错误 |
| 逻辑重启 | 终止当前子任务、回收状态并重新开始 | 是 | 任务卡死或配置变更后重跑 |
| 清除错误并重试 | 清空连续失败计数并恢复运行 | 视当前状态 | 修复外部故障后重新尝试 |

优先使用“排空后暂停”进行常规维护。只有当前任务不应继续时才使用“立即停止”。

### 6.4 服务运行和调度状态不是一回事

VPS 上 `ccusr-pipeline.service` 显示 `active (running)`，只表示守护进程在线。页面显示“已停止”时，守护进程仍会驻留并持续更新心跳，但不会抓取新闻、创建批次或运行题目。

这个设计使页面可以随时发送新的“开始”指令，不需要先登录 VPS 启动 Python 进程。

页面状态行会显示当前运行模式：“完整流水线”或“只出题+质检”。运行模式保存在 `production.sqlite3`，暂停、停止和服务重启不会丢失；点击“开始”会明确切回完整流水线。

## 7. 页面信息如何理解

### 7.1 调度器状态

- `running`：正在运行生产周期。
- `paused`：已暂停，不会开始新周期。
- `draining`：正在等待当前周期结束，然后暂停。
- `stopped`：停止接单；守护进程可以仍然在线。
- `restarting`：正在中断当前子任务并执行逻辑重启。
- `error`：本轮失败或触发了安全保护。

页面同时展示“目标状态”和实际心跳。心跳超过约 15 秒会被视为守护进程离线，此时应先检查 systemd/launchd 服务，而不是反复点击开始。

### 7.2 阶段

- `news`：抓取并登记新闻主题。
- `author`：Codex 出题、建仓和出题质检。
- `model`：Claude 模型池和 Codex 交付池正在流水处理。
- `delivery`：交付记录生产。
- `delivery_qc`：交付质检。
- `export`：Excel 和 JSONL 导出。
- `idle`：等待控制指令或下一轮。
- `error`：当前周期失败。

优化后的常驻流程会让模型和交付重叠运行，所以总览阶段可能仍显示 `model`，但批次进度里的“模型、记录、质检”三个计数会分别增长。逐题细节以原始日志为准。

### 7.3 批次进度

- “模型 X/Y”表示已经完成 Claude 运行的题目数。
- “出题质检完成”表示批次处于 `ready`，尚未启动 Claude。
- “记录 X/Y”表示已经生成至少一条交付记录的题目数。
- “质检 X/Y”表示交付记录已经通过质检的题目数。
- “部分交付”表示合格题已经导出，但批次里有永久失败题。
- “无可交付题目”表示该批次没有任何满足导出门槛的题。

## 8. 多 VPS 管理

### 8.1 为每台 VPS 建立独立隧道

每个节点使用不同本地端口：

```bash
ssh -N -L 18787:127.0.0.1:8787 ubuntu@VPS_1_IP
ssh -N -L 18788:127.0.0.1:8787 ubuntu@VPS_2_IP
ssh -N -L 18789:127.0.0.1:8787 ubuntu@VPS_3_IP
```

这三个命令需要分别保持运行。建议为 VPS 配置 SSH Key，避免隧道重连时重复输入密码。

### 8.2 在页面登记节点

打开本机控制台左侧“VPS 管理”，为每台 VPS 填写：

- 节点名称，例如 `VPS-01`。
- 控制台地址，例如 `http://127.0.0.1:18787`。
- SSH 隧道命令，仅作为操作提示保存。
- 是否启用。

保存后，“VPS 管理”可以查看远程批次和下载交付包；“调度器”可以在本机和各 VPS 之间切换，查看完整生产现场并发送控制指令。

### 8.3 修改某台 VPS 的运行配置

本机控制台的“运行配置”只修改本机 `.env`。要修改 VPS 的新闻来源、Token、难度、批次大小或并发，请在“VPS 管理”中点击“打开原始页面”，进入该 VPS 的隧道地址，再打开它自己的“运行配置”。

每台 VPS 应使用自己的 `CC_GITHUB_TOKEN` 和 GitHub 账号。Token 必须有当前项目和题目仓库所需权限，但不要写进节点 URL、SSH 命令、Git Remote 或日志。

## 9. 并发配置建议

并发目标是最大化最终通过质检的交付物，而不是让 CPU 始终达到 100%。Claude 容器、Codex 交付、Docker、SQLite 和操作系统需要共享资源。

| 机器配置 | 质检并发 | 模型并发 | 交付并发 | 建议每批题数 |
| --- | ---: | ---: | ---: | ---: |
| 2 核 / 8 GB | 2 | 2 | 1 | 10 |
| 4 核 / 16 GB | 2 | 3 | 1-2 | 10 |
| 8 核 / 32 GB | 2 | 6 | 2 | 10 |

8 核 32 GB 建议从 `2 / 6 / 2` 开始。运行至少 24 小时后，同时满足下面条件才尝试把模型并发调到 7：

- CPU 长期低于 75%-80%。
- 内存长期低于 70%。
- 磁盘 I/O 没有明显等待。
- Claude 接口没有明显 429、超时或连接错误。
- Codex 交付队列没有持续积压。
- 最终交付通过率没有下降。

不建议直接把 8 核机器的模型并发设为 8。最坏情况下 8 个容器会占满 8 个 CPU 配额，使交付、数据库和 Docker 管理进程得不到足够资源，最终交付吞吐量反而可能下降。

## 10. 日志和可观测性

### 10.1 页面日志

“调度器”页面提供两类日志：

- “事件”：结构化记录周期开始、主题领取、阶段切换、停止、重启、安全保护和失败。
- “原始日志”：读取 `runs/daemon/` 中最近日志的末尾内容。

可以按级别、阶段和关键词筛选，也可以下载当前筛选结果。远程节点日志同样通过本机控制台代理读取。

### 10.2 文件位置

```text
runs/daemon/author-<批次>.log        Codex 出题和出题质检日志
runs/daemon/workers-<批次>.log       模型与交付流水线总日志
runs/<批次>/<任务ID>/docker-*.log    单次 Claude 容器输出
runs/<批次>/<任务ID>/<run-id>/       单次尝试的 Claude 状态和 HOME
runs/<批次>/<任务ID>/delivery-*.log  逐题交付生产/质检日志
runs/<批次>/export.log               批次导出日志
```

VPS systemd 日志：

```bash
journalctl -u ccusr-pipeline.service -f
journalctl -u ccusr-console.service -f
```

调度器默认清理 `runs/daemon/` 中超过 30 天的日志，并把该目录的日志总量控制在约 2 GB。题目工作目录、原始轨迹和交付产物不属于这项日志清理范围，需要单独规划磁盘归档。

## 11. 下载 Excel 和 JSONL

### 11.1 本机批次

打开“导出中心”，选择已经完成或部分交付的批次，点击“下载交付包”。浏览器会下载：

```text
ccusr-delivery-<批次>.zip
```

ZIP 中只包含批次根目录已经登记的：

- `CC_Codex*.xlsx`
- `轨迹_*.jsonl`

每次成功下载都会在数据库记录下载次数和最近下载时间，页面会显示“已下载”标识。

### 11.2 VPS 批次

打开本机“VPS 管理”，选择在线节点和批次，点击“下载交付包”。远程控制台在 VPS 内存中生成 ZIP，本机控制台代理下载，不需要使用 `scp` 手动查找文件。

如果节点离线，先确认 SSH 隧道仍在运行，再确认 VPS 的 `ccusr-console.service` 正常。

## 12. 安全保护和自动恢复

### 12.1 连续失败熔断

默认连续 3 个周期失败后，调度器自动进入错误/暂停状态，不再开始新周期。修复 API、Token、Docker、Codex 或 GitHub 问题后，点击“清除错误并重试”。

### 12.2 磁盘保护

默认可用磁盘低于 15 GB 时自动暂停，并在页面显示错误。清理或扩容后再重试。不要只删除 `production.sqlite3`；它是批次、题目、运行、质检和调度状态的唯一数据源。

### 12.3 SQLite 并发

数据库已启用 WAL、30 秒 busy timeout 和外键检查，以支持模型池与交付池并发写入。仍应避免手工脚本长时间占用写事务，也不要在共享文件系统上让多台机器同时打开同一个 SQLite 文件。

### 12.4 中断恢复

调度器启动时会：

- 把未结束的旧周期标记为 `interrupted`。
- 回收数据库中仍标记为运行的 Claude 容器。
- 将对应题目放回可恢复状态。
- 在下一次模型重试前恢复 Git 初始快照。

不要手工把失败题直接改成 `completed`，否则交付阶段可能读取到不完整轨迹或脏工作区。

## 13. 本机常驻运行

macOS 可以安装 launchd 服务：

```bash
chmod +x deploy/install_macos_services.sh
./deploy/install_macos_services.sh
```

安装后：

- 控制台默认监听 `127.0.0.1:4173`。
- 调度器和控制台会在登录时启动，并在异常退出后重新拉起。
- 电脑关机、休眠或 Docker Desktop 停止期间，本机不会生产。
- 本机新闻来源和 VPS 新闻来源互不影响。

安装服务不等于必须立即生产。打开页面确认配置后，再从“调度器”点击“开始”。

## 14. VPS systemd 管理

安装或更新服务文件：

```bash
sudo install -m 0644 deploy/systemd/ccusr-pipeline.service /etc/systemd/system/ccusr-pipeline.service
sudo install -m 0644 deploy/systemd/ccusr-console.service /etc/systemd/system/ccusr-console.service
sudo systemctl daemon-reload
sudo systemctl enable --now ccusr-pipeline.service ccusr-console.service
```

全新数据库中的调度器目标状态默认为 `running`，因此首次执行 `enable --now` 可能立即开始生产。如果希望先检查配置，可以只启动控制台，在“调度器”页面发送“立即停止”，确认目标状态为 `stopped` 后再启动 `ccusr-pipeline.service`：

```bash
sudo systemctl enable --now ccusr-console.service
# 通过控制台把调度器设为 stopped
sudo systemctl enable --now ccusr-pipeline.service
```

检查状态：

```bash
systemctl status ccusr-pipeline.service --no-pager
systemctl status ccusr-console.service --no-pager
curl -I http://127.0.0.1:8787/
```

仓库内默认 systemd 文件使用：

```text
用户：ubuntu
项目目录：/home/ubuntu/UserSatisfactionRating-codex
控制台：127.0.0.1:8787
```

如果 VPS 用户或项目目录不同，必须先修改两个 service 文件中的 `User`、`Group`、`WorkingDirectory` 和 `ExecStart`。

## 15. 安全部署更新流程

更新代码前，不要在调度器运行中直接 `git pull`。推荐操作顺序：

1. 页面点击“排空后暂停”，等待当前周期结束。
2. 确认页面没有运行中的 Docker 容器。
3. 停止调度器和控制台服务。
4. 快进拉取指定生产分支。
5. 如 service 文件变化，重新安装并执行 `daemon-reload`。
6. 重新构建有变化的 Docker worker 镜像。
7. 启动两个服务。
8. 检查 HTTP、Git 提交、调度器心跳和数据库状态。
9. 确认无误后在页面点击“开始”。

命令示例：

```bash
cd /home/ubuntu/UserSatisfactionRating-codex
sudo systemctl stop ccusr-console.service ccusr-pipeline.service
git pull --ff-only origin codex/vps-docker-pipeline
docker build -f Dockerfile.worker -t ccusr-claude-worker:local .
sudo install -m 0644 deploy/systemd/ccusr-pipeline.service /etc/systemd/system/ccusr-pipeline.service
sudo install -m 0644 deploy/systemd/ccusr-console.service /etc/systemd/system/ccusr-console.service
sudo systemctl daemon-reload
sudo systemctl start ccusr-pipeline.service ccusr-console.service
```

已有数据库在服务重启后会保留其中的 `stopped` 或 `paused` 目标状态。此时 `systemctl start` 不代表一定开始业务生产，最终以“调度器”页面显示的目标状态为准。

## 16. 常见问题

### SSH 输入密码后没有反应

`ssh -N -L ...` 只建立隧道，不打开远程 shell，也不会持续输出日志。密码正确后终端停在那里就是正常状态。保持窗口不关闭，并访问映射后的本机端口。

### 页面显示节点离线

依次检查：

1. SSH 隧道终端是否仍在运行。
2. 节点控制台地址和本地映射端口是否一致。
3. `ccusr-console.service` 是否为 active。
4. VPS 本机执行 `curl http://127.0.0.1:8787/api/scheduler` 是否成功。
5. 防火墙或 SSH 是否断线。

### 服务 active，但没有继续出题

检查调度器页面的目标状态。常见原因是：

- 调度器处于 `stopped` 或 `paused`。
- 同机手动任务仍显示为 queued/running。
- 连续失败达到熔断阈值。
- 剩余磁盘低于安全阈值。
- 新闻来源全部失败且数据库没有待用主题。
- 存在尚未终结的旧批次，调度器正在优先恢复它。

### 为什么进度长期显示“模型阶段”

优化后的常驻调度器会让 Claude 模型池与 Codex 交付池重叠运行，`model` 是这一段流水线的总阶段。查看“模型、记录、质检”三个计数，或切换到原始日志判断具体正在处理哪一步。

### 为什么有题失败，批次仍然导出了

这是预期行为。永久失败题标记为 `blocked`，通过交付质检的题正常导出，批次标记为 `partial`。ZIP 中不会包含失败题。

### 修改配置后是否需要重启服务

题数、难度、新闻来源、模型并发、交付并发和 worker 镜像会在后续周期重新读取，通常不需要重启。正在运行的容器不会中途改变配置。为了让变更边界清晰，建议先“排空后暂停”，保存配置，再点击“开始”。

## 17. 日常检查清单

每天建议检查一次：

- 调度器心跳正常，连续失败数为 0。
- 新闻待用数量不是长期为 0。
- 模型完成、交付记录、质检通过三个计数都在增长。
- 没有容器长时间停留且日志不更新。
- CPU、内存和磁盘没有长期超过阈值。
- Git 分支和提交是预期的生产版本，工作区没有未知修改。
- `partial` 和 `failed` 批次有明确失败原因。
- 已完成批次的 Excel 和 JSONL 能正常下载。
- 下载后的 ZIP 已转移到正式交付或归档位置。

每次升级、修改 Token、切换模型或提高并发后，至少观察一个完整批次，再决定是否继续扩大负载。
